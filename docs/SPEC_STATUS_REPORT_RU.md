# Отчёт соответствия — Kleal Matching Core v2 (как есть)

_Проверено по реальному коду (15 агентов, сверка с shared/kleal_*, services/matching, core_v2, config, 264 теста). Только фактически сделанное — без будущего времени._

## Сводка (213 подпунктов)

| Статус | Кол-во | Что значит |
|---|--:|---|
| ✅ done | 84 | |
| 🟡 partial | 80 | |
| ⛔ pilot_disabled | 24 | |
| 🔒 sha_pinned | 11 | |
| ⬜ not_done | 14 | |

## §0–§3 Резюме/решения, Цель и границы, Нормативные принципы, Архитектура решения

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **§0.0** Резюме и решения — шесть независимых слоёв + запрет компенсации | реализовано | Шесть слоёв реализованы как раздельные модули/шаги (eligibility §8 в app.py, retrieval §7, relevance §9 в core_v2, reciprocity/readiness §10, allocation §11, transaction §13/§14), и высокий fit не отменяет блокировку: гейты возвращают ALLOW/REVIEW/BLOCK по _DEC_RANK и режутся до скоринга. — «services/matching/app.py:679 _DEC_RANK; core_v2.py score layers» |
| ✅ | **§0.1** Ключевое решение: веса/пороги только в versioned YAML | реализовано | Все веса и пороги читаются из sha-pinned config/Kleal_Matching_Core_Config_v2.yaml через core_v2.load_config (валидируется config/schema.json draft-07), текст спеки не является вторым источником. — «services/matching/core_v2.py:load_config; config/*.yaml,schema.json» |
| ✅ | **§0.2** Ключевое решение: semantic_tier как происхождение кандидата, независим от score | реализовано | core_v2 присваивает T0–T3 (TIER_KIND: reciprocal/exact/related/adjacent) по пути обнаружения, отдельно от band/relevance; тир не входит в формулу score. — «services/matching/core_v2.py:371 TIER_KIND» |
| ✅ | **§0.3** Ключевое решение: unknown/mismatch/not_applicable — разные состояния | реализовано | В core_v2 unknown даёт доменный prior и снижает coverage (не match), а not_applicable исключён из знаменателя coverage; unknowns собираются в список. — «services/matching/core_v2.py:12-14,347-350» |
| ✅ | **§0.4** Ключевое решение: relevance — внутренняя эвристика 0–1, не процент совместимости | реализовано | relevance считается как R_mean/R_lcb в диапазоне 0–1 и наружу отдаётся качественными band'ами, а не как процент; сырой процент подавлен (см. §9.7 fix в git-истории). — «services/matching/core_v2.py:5-6,473» |
| 🟡 | **§0.5** Ключевое решение: пользователю — уровень, 2–3 причины, один gap, объяснение расширения | частично | explanation_keys/подтверждённые причины и gaps генерируются (§18 presentation в app.py), band + tier-объяснение отдаются; строгий лимит «2–3 причины/один gap» как жёсткое правило в этом срезе не подтверждён отдельным тестом. — «services/matching/app.py presentation; core_v2 explanation» |
| ✅ | **§0.6** Ключевое решение: agent-to-agent — структурированный протокол, не свободный LLM-диалог | реализовано | shared/kleal_protocol.py реализует детерминированный keyless структурированный протокол переговоров (тесты NEG1-4), без свободных LLM-диалогов в скоринге. — «shared/kleal_protocol.py; test_core_v2.py NEG1-4» |
| ⛔ | **§0.7** Ключевое решение: групповой matching — отдельная set-level задача; события/комнаты — отдельные типы | pilot-выкл (scaffolding) | group_formation реализован как conformance-scaffolding в shared/kleal_groups.py (алгоритм гоняется только под override enable_group_formation, ответ всегда enabled:False), а intent_to_event/room/venue — отдельные типы в DECISION_TYPE_OF_KIND, но выключены в пилоте. — «shared/kleal_groups.py:7-9; shared/kleal_candidates.py:31» |
| 🟡 | **§0.8** Ключевое решение: dating — отдельный профиль/policy/consent/disclosure/release gate | частично | Кросс-purpose gate блокирует пары dating↔non-dating (_XPURPOSE_BLOCK) и есть disclosure-ladder, но отдельный dating-safety/legal release-gate как включаемый контур в этом срезе не выделен отдельным модулем. — «services/matching/app.py:677 _XPURPOSE_BLOCK» |
| ✅ | **§0.9** Ключевое решение: все переходы proposal/match/plan транзакционны, идемпотентны, ре-валидируют policy/TTL/capacity/версии | реализовано | kleal_states.py даёт 4 машины состояний, CAS по version, idempotency dedup, unique-active-pair и таблицу race-разрешений; app.py на send/accept-границе делает revalidate() под reentrant-локом (Plan-машина определена, enabled:False). — «shared/kleal_states.py; services/matching/app.py:1613,1788» |
| ✅ | **§0.10** Ключевое решение: оплата подписки не влияет на relevance/eligibility/safety/порядок | реализовано | PAYMENT_INVARIANT в app.py фиксирует и enforce'ит, что подписка меняет лимиты/инструменты, но никогда relevance/eligibility/safety/порядок выдачи. — «services/matching/app.py:540-542 PAYMENT_INVARIANT» |
| ✅ | **§0.11** Таблица выходов системы (policy_decision, semantic_tier, relevance_score, evidence_coverage, reciprocal_relevance, readiness_state, allocation_action, explanation_keys) | реализовано | Все восемь выходов присутствуют в коде: policy_decision (ALLOW/REVIEW/BLOCK), semantic_tier T0–T3, relevance 0–1, coverage+unknowns, reciprocal 0.7*min+0.3*mean, readiness (READINESS_RANK open_now..busy), typed allocation_action (§0-таблица в app.py:763), explanation_keys. — «services/matching/app.py:679,763; core_v2.py:352,385» |
| ✅ | **§1.0** Цель и границы Matching Core — набор проверяемых промежуточных решений, не одна псевдовероятность | реализовано | Пайплайн реализован как цепочка отдельных проверяемых решений (gates→tiers→relevance→readiness→allocation→transaction), а не как единая вероятностная формула; таблица контуров §1 (intent/eligibility/retrieval/relevance/reciprocity/allocation/transaction) отражена соответствующими модулями. — «services/matching/app.py plan/match pipeline; core_v2.py» |
| 🟡 | **§1.0b** Инвариант успеха: completed interaction с двусторонним feedback; отсутствие feedback не негатив | частично | Feedback-loop (file-backed kleal_store.json) и readiness учитывают недоставку/молчание (app.py:1593 'не считать молчание отказом, если не доставлено'), но «completed interaction» как терминальный success-исход в этом срезе не моделируется явной метрикой. — «services/matching/app.py:296-298,1593» |
| 🟡 | **§1.1** Поддерживаемые типы решений (person↔person, intent↔intent, group, event, room, continuation) | частично | PILOT_DECISION_TYPES объявляет все 7 типов: person_to_person=True и intent_to_intent=True (живые; полный condition-merge частичный), а group_formation/intent_to_event/intent_to_room/intent_to_venue/relationship_continuation объявлены, но =False. — «services/matching/app.py:549-557» |
| ⛔ | **§1.2** Границы пилота — домены через release flags; Барселона active-intent; dating отдельный gate | pilot-выкл (scaffolding) | RELEASE_STATUS берётся из pinned config, _pilot_enabled/_decision_type пропускают только пилотные типы и отдают явное «не в этом пилоте» для остального; per-домен release-флаги для social_meet/walk/… как отдельный включаемый список в этом срезе не подтверждены отдельно от decision-type surface. — «services/matching/app.py:548,558-571» |
| ✅ | **§2.0** Нормативные принципы и термины (Intent/Eligibility/Semantic tier/Directional relevance/Evidence coverage/Receiving policy/Readiness/Match/Success) | реализовано | Термины материализованы в коде: intent §5 (kleal_intent), eligibility §8 gates, semantic tier T0–T3, directional relevance A→B и B→A, evidence coverage+unknowns, receiving policy/fatigue (app.py:296), readiness (READINESS_RANK), match после revalidation. — «shared/kleal_intent.py; core_v2.py:318,385; app.py:296» |
| ⬜ | **§2.0b** Нормативные слова ОБЯЗАТЕЛЬНО/РЕКОМЕНДУЕТСЯ/ДОПУСКАЕТСЯ/ЗАПРЕЩЕНО | не реализовано | Это глоссарий нормативной терминологии спеки; отдельного кодового артефакта не требует и не имеет. |
| ✅ | **§3.0** Архитектура принятия решения — 9-шаговая последовательность (Intent Compiler → … → Transaction Orchestrator) | реализовано | Все девять шагов присутствуют: compile_intent §4.3 (kc.compile_intent), policy view/ALLOW-BLOCK-REVIEW, retrieval+tier, feature/evidence groups без double-count, relevance A→B/B→A+coverage+conservative, readiness/receiving, allocation slate/waves, presentation reasons/gaps, transaction revalidation на каждом переходе. — «services/matching/app.py:1214,1613,1788; core_v2.py:347-352,572» |
| ✅ | **§3.0b** Запрещённая архитектура: единый FinalUtility со сложением relevance/safety/fairness/оплаты/fatigue | реализовано | Единого FinalUtility в коде нет: safety/eligibility — отдельные булевы гейты до скоринга, relevance/reciprocal/coverage/band считаются раздельно, оплата исключена инвариантом, аллокация derive'ится из уже принятых решений, а не из нового суммарного score. — «services/matching/app.py:679,763; PAYMENT_INVARIANT:540» |

## §4 Канонические контракты данных (4.0–4.4)

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **§4.0** Интро: 12 канонических сущностей + immutable snapshot с версиями data/policy/config | реализовано | Все 12 сущностей таблицы §4 реализованы как детерминированные keyless-билдеры в shared/kleal_contracts.py (build_user_profile, build_profile_view, build_receiving_policy, compile_intent, build_evidence, build_candidate_snapshot, build_proposal, build_reservation, build_match, build_group, build_plan, build_relationship_edge); CandidateSnapshot несёт версии intent_version/config_version/config_sha (kleal_contracts.py:396-402), а слой стемпится в matching через _stamp_contracts (app.py:1273-1289). — «shared/kleal_contracts.py; services/matching/app.py:1235,1273» |
| ✅ | **§4.1** Evidence object + общий evidence_id для алиасов/тегов/нод из одной фразы (дедуп) | реализовано | build_evidence (kleal_contracts.py:359-376) выдаёт объект со всеми полями спеки (source/scope/confidence/freshness/sensitivity/allowed_purposes/visibility/last_confirmed_at/expires_at); evidence_id детерминирован через _det_id по (field,value,source,scope), поэтому дериваты одной фразы совпадают — покрыто тестом C4-EV1 (test_core_v2.py:508-512), а на уровне таксономии алиас и каноника делят один id (C6-EVID-ONE-ID, test_core_v2.py:740). — «shared/kleal_contracts.py:109-113,359-376» |
| ✅ | **§4.2** Иерархия источников (7 уровней) с правилом перекрытия и запретом скрытого hard-gate из inference | реализовано | SOURCE_HIERARCHY кортеж из 7 уровней в точном порядке спеки + SOURCE_PRIORITY и resolve_source (перекрытие по приоритету) в kleal_contracts.py:22-35; FEATURE_GROUP_SOURCE (строки 40-48) раздаёт per-feature-group провенанс так, что vibe=agent_inference(L7), а неизвестный источник в build_evidence принудительно опускается до agent_inference (строка 366); проверяется C4-EV3/C4-INT4/C4-INT5 (test_core_v2.py:514,551-554). — «shared/kleal_contracts.py:22-48,366» |
| ✅ | **§4.3** Intent schema: 11 блоков (Identity/Goal/Time/Location/Mode&format/Target/Social/Domain details/Fallback/Disclosure/Lifecycle) + версия и TTL | реализовано | compile_intent (kleal_contracts.py:269-324) штампует все 11 §4.3-блоков поверх 16 плоских ключей движка без их перезаписи (хранение времени в UTC+исходной timezone, urgency через _urgency, fallback.allowed_dimensions, disclosure.stage_map, lifecycle с created_at/expires_at/ttl_seconds/search_budget); истёкший intent исключается из ранжирования через is_expired (строки 336-346, вызовы app.py:1212,2108); Domain details/target.required_roles присутствуют как контрактные блоки, но заполнены плейсхолдерами (platform/server/rank=None, required_roles=[]). — «shared/kleal_contracts.py:269-346; services/matching/app.py:1212,1214» |
| ✅ | **§4.4** Receiving policy (status/allowed_domains/passive/quiet_hours/proposal_budget/location_scope/allowed_proposal_types/disclosure_stage/paused_until) | реализовано | build_receiving_policy (kleal_contracts.py:190-235) собирает канонический полис из admin-полей rcv* со всеми расширенными §4.4-ключами и сохраняет None-семантику исходного _receiving_from_form (partial patch пропускает, cleared full save -> None); эти поля реально потребляются на send-boundary в _receiving_send_gate (app.py:1566-1588: allowed_proposal_types, location_scope, per_7d budget) через _pre_send_revalidate (app.py:1644); покрыто C4-RECV1..RECV4 (test_core_v2.py:482-491). — «shared/kleal_contracts.py:190-235; services/matching/app.py:1566-1588,1644» |

## §5 Intent Compiler (5.1–5.3) + §6 Таксономия (6.1)

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **§5.0** Intent Compiler: LLM как parser, output недоверенный (schema/allowlist/normalizer/policy) | реализовано | kleal_intent.validate_and_normalize() трактует intent как недоверенный: достраивает 16 flat-ключей из kc._FLAT_DEFAULTS, allowlist-ит type/role/mode (off-list -> deny-safe social/meet/offline), клампит числа fail-open, обрезает языковые коды до 2 символов, коэрсит булевы, идемпотентна и не выбрасывает; вызывается на входе match_candidates и explain_match (PARITY) и внутри _parse_query app.py. — «shared/kleal_intent.py:validate_and_normalize/normalize_for_scoring; services/matching/app.py:1210,1902; tests C5 lines 593-601» |
| ✅ | **§5.1** Hard и soft: таблица фраз + правило подтверждения sensitive-ограничений | реализовано | extract_constraints() магнитит исходный free-text на ADDITIVE-ключи для всех 5 строк таблицы («рядом»->soft preferredNearby, «только по-испански»->proposedRequiredLanguages hard_after_confirm, «без токсиков»->moderationPrefs, «можно онлайн»->allowOnlineFallback, «вторую половинку»->proposedType=dating), не трогая 16 gate-ключей; intent_summary() помечает requires_confirmation для sensitive/large-share/safety, а apply_confirmation() промоутит hard-gate только после подтверждения id. — «shared/kleal_intent.py:extract_constraints/intent_summary/apply_confirmation; tests lines 608-641» |
| ✅ | **§5.2** Операционализированная clarification policy: P0–P3, rule-based один вопрос | реализовано | clarification_policy() детектит гэпы классов P0 (dating_opt_in/platform_crossplay/required_language), P1 (time/location/format/language_level), P2 (skill/team/rank), P3 (card_title), сортирует по (класс, -safety, -pool_split, -supply, -control, friction) БЕЗ формулы EVI, выбирает не более одного вопроса и откладывает всё кроме mandatory-safety до первых результатов (_is_mandatory_safety: safety_impact==2 или dating_opt_in). — «shared/kleal_intent.py:_detect_gaps/_is_mandatory_safety/clarification_policy; app.py:1478; tests lines 647-657» |
| ✅ | **§5.3** Минимально достаточный intent (чеклист 7 пунктов) | реализовано | minimally_sufficient() — verification-only чеклист над уже скомпилированными §4-блоками: проверяет domain+activity+purpose, time-горизонт (Flexible=явное no-time), mode+format, city/online-fallback, domain-critical поля через _domain_critical(), fallback+disclosure stage_map, ttl_seconds+search_budget; возвращает {ok, missing, present}, ничего не штампует. — «shared/kleal_intent.py:minimally_sufficient/_domain_critical; app.py:1479; test line 666» |
| ✅ | **§6.0** Таксономия: типизированные связи (alias/exact/sibling/parent/adjacent/negative/complementary) + governance | реализовано | kleal_taxonomy.py — строго READ-ONLY нарратор над запечатанным core_v2: edge_type() классифицирует пару через инжектированный topical в alias/exact_entity/direct_sibling/parent/adjacent_purpose/literal_token_share/none c advisory-зеркалами semantic_tier и sem_value, complementary() даёт role-матрицу (support↔carry, learner↔native) явно не-similarity, governance_of() навешивает 6 полей (owner/version/language_aliases/review_state/evidence/rollback), а alias_invariant()/shadow_replay() — falsifiable governance-пруфы; edge никогда не переписывает band/tier/score. — «shared/kleal_taxonomy.py:edge_type/complementary/governance_of/alias_invariant/shadow_replay; app.py:202,1291; tests C6 lines 676-749» |
| 🔒 | **§6.1** Evidence groups против double count (7 feature groups, cap 1.0, source учитывается один раз) | в движке (sha-pinned) | Семь feature-групп (semantic_activity/time_feasibility/location_feasibility/mode_format/directed_preferences/social_context/domain_constraints) заданы в sha-pinned config и FEATURE_KEYS, а анти-double-count реализован в sha-pinned core_v2.build_features(): по одной агрегированной subfeature на группу с cap 1.0, где alias/parent/exact чтения одного интереса схлопываются в один matched-set (semantic_activity даёт единый SEM_VALUE[best], а не сумму), — сам §6-слой kleal_taxonomy лишь нарратор и в этот подсчёт не вмешивается. — «config/Kleal_Matching_Core_Config_v2.yaml:8-43; services/matching/core_v2.py:33 FEATURE_KEYS,199 build_features; kleal_taxonomy narration only» |

## §7 Candidate retrieval + §8 Eligibility/privacy/safety/purpose-binding (AS-IS)

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **§7.0** Candidate retrieval и semantic tiers (интро) | реализовано | match_candidates() сначала прогоняет весь пул через _policy_decision (scoring только после не-BLOCK), затем _retrieve() собирает пул, а core_v2.search присваивает immutable-tier T0..T5; retrieval-слой read-only поверх запечатанного скорера. — «services/matching/app.py:1203 match_candidates; :870 tier labels; core_v2.assign_tier» |
| 🟡 | **§7.1** Порядок источников + таблица semantic tiers | частично | RETRIEVAL_SOURCES кодирует 5 источников: 1 (active_intent reciprocal), 2 (direct interest best>=4), 5 (parent/adjacent) — включены; источники 3 (groups) и 4 (events/rooms) объявлены, но enabled:False (pilot-disabled, retrieve 0 кандидатов); _retrieve() лейблит каждого через _retrieval_source и сортирует по _SOURCE_RANK 1->2->5->0; tier как immutable provenance (никогда не переприсваивается), per-tier аналитика в _tier_analytics; тест C7-SOURCE/C7-SOURCE2. — «app.py:1122 RETRIEVAL_SOURCES; :1130 _retrieval_source; :1150 _retrieve; :1171 _tier_analytics; test_core_v2.py:787-793» |
| 🟡 | **§7.2** Retrieval budgets (семиэтапный конвейер) | частично | RETRIEVAL_STAGES перечисляет все 7 этапов; hard_prefilter реализован как in-memory policy-gates+geo (SQL/PostGIS/H3 помечены как отложенная инфра), structured_retrieval и feature_build/ranking_slate реальны; ann_recall (pgvector) помечен status:blocked_infra и не выполняется; бюджет RETRIEVAL_BUDGET=500 при ~100 юзерах не срезает — тесты C7-BUDGET-NOOP и C7-BUDGET-BITE доказывают, что срез удаляет только no-overlap (T5) хвост, слейт байт-идентичен. — «app.py:1107 RETRIEVAL_BUDGET; :1110 RETRIEVAL_STAGES; :1228 budget; test_core_v2.py:775-789» |
| ✅ | **§8.0** Eligibility/privacy/safety/purpose binding (интро: ALLOW/BLOCK/REVIEW) | реализовано | _policy_decision — трёхзначное решение: BLOCK исключает кандидата из eligible до скоринга (match_candidates пропускает его), REVIEW делает кандидата discoverable, но не auto-proposable (_allocation_action -> review_required), ALLOW допускает outreach; ranking запускается только над не-BLOCK пулом. — «app.py:735 _policy_decision; :1216-1224 eligible loop; :762 _allocation_action» |
| 🟡 | **§8.1** Канонические gates | частично | _hard_gates покрывает account_status (_ACCOUNT_BLOCK+suspended), age/18+ и min/maxAge, mutual block (blocked/blocksMe), privacy visibility=private, intent-mode isolation через _cross_purpose_blocked, language feasibility (requiredLanguages), capacity/fatigue (pending>=MAX_PENDING, cooldown), safety restrictions (_SAFETY_BLOCK жёстко + sensitivity/review->REVIEW), radius; time feasibility на send-boundary (_time_feasible); disclosure policy через clamp; каждый gate ADDITIVE и default-ALLOW при отсутствии поля; тесты C8-ACCT/C8-PRIV/C8-SAFETY/C8-XPURP. Location-policy как отдельный zone-expansion gate реализован только опционально через soft_eligibility (REVIEW), иначе radius -> BLOCK. — «app.py:701 _hard_gates; :735 _policy_decision; :687 _cross_purpose_blocked; test_core_v2.py:825-852» |
| ✅ | **§8.2** Точки обязательной повторной проверки + POLICY_CHANGED | реализовано | revalidate() переисполняет (intent,candidate) над LIVE store на чекпоинтах _REVAL_CHECKPOINTS (slate/send/profile_open/accept/shared_chat/reveal_place/reveal_contact/state_change) и возвращает code=POLICY_CHANGED, если live-решение строже базлайна; send-boundary дублирует стек в _negotiate_precheck (hard_gates+cross-purpose+outreach), выдавая POLICY_CHANGED; тесты C8-REVAL, C14-RACE (privacy_tightened/policy_revoked->POLICY_CHANGED). — «app.py:680 _REVAL_CHECKPOINTS; :1712 revalidate; :1591 _negotiate_precheck; test_core_v2.py:869-878» |
| ✅ | **§8.3** Контекстные профили и purpose binding | реализовано | kc.build_profile_view даёт purpose-bound минимальную проекцию по DOMAIN_TO_CONTEXT (6 контекстов) с allow-list PURPOSE_FIELDS = комплемент deny-колонок (friendship/networking/... без datingOk, dating без entities); неизвестный домен -> deny-safe (только name); каждая карта слейта получает profile_view в _stamp_contracts; Match Capsule (kc.build_match) создаётся под конкретный purpose_id с version и ttl_seconds=MATCH_TTL; тесты C4-ENUMS, C4-INT2, C4-DENY. — «shared/kleal_contracts.py:70 CONTEXTS; :85 PURPOSE_FIELDS; :171 build_profile_view; :439 build_match; app.py:1289; test_core_v2.py:493-505» |
| 🟡 | **§8.4** География и время | частично | _PRECISE_LOCATION_KEYS (home/work/address/exact_lat...) вычищаются из каждой карты в _stamp_contracts (сегодня no-op — фикстуры их не несут); retrieval использует радиус/km, а не точную live-локацию; время нормализуется в UTC через _to_utc_min по tz_offset_min, слот-feasibility с travel_buffer_min в _time_feasible (absent-permissive); точное место раскрывается только после mutual Match Capsule + full disclosure (_revalidate_disclosure); H3-cell/зоны и DST-таблица/travel-mode роутинг НЕ реализованы (помечены как инфра); тесты C8-TIME, C8-REVEAL. — «app.py:684 _PRECISE_LOCATION_KEYS; :1297 scrub; :1675 _to_utc_min; :1683 _time_feasible; :1700 _revalidate_disclosure; test_core_v2.py:880-891» |

## §9 Математическая модель: relevance, evidence и uncertainty

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **§9.0** Числовые слои не смешиваются в псевдопроцент | реализовано | Движок держит policy(ALLOW/BLOCK)/tier(провенанс)/relevance(mean,coverage,lcb)/reciprocal/readiness/band как раздельные поля карточки и score_semantics в config, ни один код не сворачивает их в единый процент (core_v2.search строки 598-616, config строки 242-251). — «core_v2.py:search L598-616; config score_semantics L242-251» |
| 🔒 | **§9.1** Четыре состояния признака | в движке (sha-pinned) | core_v2 задаёт ровно 4 состояния K_MATCH/K_MISM/UNKNOWN/NA (L167); directional_score исключает NA из знаменателя, а UNKNOWN добавляет prior и не поднимает coverage (L336-345); тесты C1c/C1d/C2/C9-NA-EXCLUDED подтверждают. — «core_v2.py L167,336-345; test C2,C9-NA-EXCLUDED L924» |
| 🟡 | **§9.2** Нормализация наблюдения (confidence×observed + (1−conf)×prior) | частично | Для unknown adjusted_value=prior и для NA вес=0 реализованы точно (core_v2.directional_score L340-345), но для known значение входит СЫРЫМ (confidence_k≡1, без смешивания с prior) — что тест C9-FORMULA явно фиксирует как принятое допущение; отдельного per-observation confidence поля движок не читает. — «core_v2.py:directional_score L340-345; test C9-FORMULA L910-923» |
| 🔒 | **§9.3** Relevance mean, evidence coverage и R_lcb | в движке (sha-pinned) | directional_score вычисляет R_mean=Σ(w·adj)/Σw, Coverage=Σ(w·known)/Σw и R_lcb=clamp(mean−λ·(1−cov),0,1); λ_domain берётся из config uncertainty_lambda (dating=0.35 выше passive), тест C9-FORMULA сверяет формулы побитово. — «core_v2.py:directional_score L327-350; config uncertainty_lambda; test C9-FORMULA» |
| 🔒 | **§9.4** Направленное и двустороннее (reciprocal) соответствие | в движке (sha-pinned) | search считает d_ab и d_ba (reverse_features строит псевдо-intent B→A), reciprocal_score=round(0.7·min+0.3·mean,4) над lcb-оценками, bounded [0,1]; P_accept/P_response/P_completion нигде не вычисляются, тест C9-RECIP подтверждает формулу и отсутствие утечки вероятности. — «core_v2.py:reciprocal_score L352-355, search L570-572; test C9-RECIP L928» |
| ✅ | **§9.5** Канонические веса доменов + CI-проверки config | реализовано | Веса живут только в config (sha-pinned), load_config проверяет сумму весов домена=1.0, отсутствие неизвестных feature keys и диапазоны priors/thresholds (core_v2 L112-135); app._validate_math_config добавляет уникальность evidence_id, совместимость config↔policy↔schema версий и диапазоны band-cut (app.py L796-822); schema.json — draft-07 контракт; тесты C9-CI-OK/PURE/VER/SCHEMA/BAND/EVID зелёные. — «core_v2.py:load_config L112-135; app.py:_validate_math_config L796-822; config/schema.json; test C9-CI-*» |
| ✅ | **§9.6** Decision thresholds пилота (5-way классификация) | реализовано | app._decision_class выдаёт strong_personal_candidate/usable_personal_candidate/discovery_only/clarification/no_personal_outreach, но не ре-порогирует по иллюстративным 0.72/0.58/0.45 — деривирует из уже проставленных движком {policy,tier,band,can_outreach,why_no_outreach}, реальные пороги берутся из per-domain floors config; стемпится на карточки (app.py L771-794,1255) и в explain (L1990); тесты D-класса L951-964. — «app.py:_decision_class L771-794; test L951-964» |
| ✅ | **§9.7** Что показывается пользователю (бэнды, не проценты) | реализовано | core_v2 присваивает 4 бэнда (especially_close/strong_option/broader_option/needs_clarification) по (lcb,coverage) из config user_facing_bands с RU/EN метками + 2-3 подтверждённые причины (только known_match) и один gap (core_v2 assign_band L470-475,_presentation L497-525); UI profile mBand (app.py L1096-1101) и match-карточка показывают качественный тег, сырой процент не выводится (коммит fcdc31b), buddy тоже без процента. — «core_v2.py:assign_band L470-475,_presentation L497-525; profile/app.py:mBand L1096-1101» |

## §10 Reciprocity/readiness (10.1–10.3) + §11 Allocation/fairness (11.1–11.3)

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **§10.0** Relevance и Readiness не складываются | реализовано | Readiness вычисляется отдельно и НЕ подмешивается в relevance: readiness_state гейтит can_outreach и порядок слейта, но lcb/coverage/reciprocal остаются те же (тест RCV2b подтверждает равенство при разной готовности). — «core_v2.py:373-459 readiness_state; test_core_v2.py RCV2b (l.240)» |
| ✅ | **§10.1** Receiving readiness — шесть состояний | реализовано | core_v2.readiness_state детерминированно эмитит все шесть состояний (open_now/open_later/passive_discovery/busy/paused/unknown) из receiving-политики, тихих часов, proposal-бюджета и paused_until; шесть состояний объяснены в kleal_completion.READINESS_EXPLAIN, готовность доменно-зависима (allowed_domains); покрыто RCV1-9 и C10-READINESS. — «core_v2.py:427-459; kleal_completion.py:73-80; test RCV1-9, C10-READINESS (l.984)» |
| ✅ | **§10.2** Completion factors — прозрачные operational signals без соц-рейтинга | реализовано | kleal_completion.completion_factors возвращает read-only список именованных сигналов (availability_fresh, capacity_headroom, active_plans, proposal_budget, technical_compat), aggregate_score=None (нет единого балла человека), а safety/sensitive/single-review поля исключены по конструкции через EXCLUDES; latency/no-show/host-venue помечены not_collected; тесты C10-CF-NOAGG и др. — «kleal_completion.py:22-69; test C10-CF-* (l.990-1007)» |
| ✅ | **§10.3** Переход к ML — 6 слоёв как манифест, гейты остаются детерминированными | реализовано | kleal_ml_boundary.ML_BOUNDARY декларативно перечисляет 6 ML-слоёв со статусами future/blocked_calibration (ни один не запущен), P_response/P_accept/P_completion = absent_by_design (не подделываются), а hard_gates/privacy/purpose_binding/block/capacity/disclosure помечены model_driven:False; assert_no_model_in_gates возвращает пусто; тесты C10-ML*/C10-NOMODEL подтверждают, что гейты не делают llm_complete. — «kleal_ml_boundary.py:9-38; test C10-NOMODEL (l.1023)» |
| ✅ | **§11.0** Allocation не меняет смысл релевантности пары | реализовано | _allocate/_stamp_allocation_trace работают post-relevance/post-policy и никогда не пишут обратно в lcb/reciprocal/mean/coverage/score; при пилотных дефолтах allocation байт-идентичен слейту core_v2 (тест C11-ALLOC-NOOP). — «app.py:1044-1074; test C11-ALLOC-NOOP (l.1059)» |
| ⛔ | **§11.1** Механизмы: exposure caps, fatigue, cooldown, exploration, diversity, popularity guard, reservation, area supply, protection | pilot-выкл (scaffolding) | Все механизмы присутствуют в ALLOCATION_CONFIG, но ВЫКЛЮЧЕНЫ дефолтами (cap=1e9, quota=0, guard=1.0): per-user exposure cap, city/area supply balancing, popularity-concentration guard и protection наиболее отзывчивых (demote-only, _within_band_demote), exploration-квота, pair_cooldown_days; кусаются только под ctx['allocation'] override (тест C11-ALLOC-BITE). Diversity slate (_diversify ≤3/bucket) и proposal-fatigue (readiness→busy при received_24h≥cap) и decline-cooldown (COOLDOWN_DAYS) активны в основном тракте; reservation capacity для групп — только через §16 override. — «app.py:999-1074 ALLOCATION_CONFIG/_allocate/_within_band_demote; _diversify:873; core_v2 fatigue:443-446; test C11-ALLOC-BITE (l.1073)» |
| ✅ | **§11.2** Порядок rerank (6 шагов) | реализовано | Реализованы все 6 шагов: (1) удаление BLOCK/expired/capacity — hard-гейты; (2) сортировка по band→readiness_class→reciprocal (core_v2 out.sort l.619); (3) diversity (_diversify); (4) exposure/fatigue caps (_allocate); (5) ограниченный exploration-append (_allocate q>0); (6) фиксация propensity+причин в allocation_trace (_stamp_allocation_trace), причём propensity=None/absent_by_design; порядок open_now-before-busy покрыт RCV9. — «core_v2.py:617-619; app.py:1044-1100; test RCV9 (l.281), C11-ALLOC-TRACE (l.1081)» |
| ✅ | **§11.3** Инвариант монетизации: payment_status запрещён как feature | реализовано | payment_status/subscription не читаются ни в relevance, ни в allocation: инъекция payment_status='premium'/subscription='paid' не меняет ни состав/порядок слейта, ни бэнды, ни allocation_trace (в трейсе нет ключей payment/subscription/P_accept); тесты C11-PAY-ALLOC, C11-ALLOC-NOPROP-NOPAY и §0-dec.10 payment-тест. — «app.py:1076-1099; test C11-PAY-ALLOC (l.1095), payment tests (l.380)» |

## §12 Controlled expansion + §13 Agent protocol

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **§12.0** Controlled expansion — гарантия полезного ответа (intro) | реализовано | Поиск никогда не упирается в тупик: app._expand_fallback (services/matching/app.py:1407) при пустой выдаче пересчитывает по релаксированному _RELAXED_CFG и, если нет топикального перекрытия, отдаёт ближайших доступных людей как явную 'broader/alternative' подсказку (не случайного человека), а не пустоту. — «services/matching/app.py:1407 _expand_fallback; :516 _relaxed_cfg» |
| 🟡 | **§12.1** Принципы расширения | частично | Принципы закодированы в ПЛАНЕ expansion_ladder (app.py:1347): один axis на шаг, cheapest-first (cost_rank==step), provenance_tier сохраняется, безопасность/age/consent/block/language/purpose перечислены в never_relaxes и не ослабляются, каждый шаг несёт explanation_ru/en, parent/adjacent-outreach помечен broad_consent_required=True; НО реально ИСПОЛНЯЕМЫЙ _expand_fallback релаксирует сразу две оси (adjacentAllowed+exactMatchRequired) и тегируется постфактум — то есть 'один axis за шаг' держится только в декларативном плане, а не в исполнении (это честно задокументировано в docstring и в principles.executed_fallback). — «app.py:1347 expansion_ladder; :1372 broad-consent note; :1415 multi-axis relax» |
| ✅ | **§12.2** Типовой порядок (7-шаговая лестница) | реализовано | Ровно 7 упорядоченных шагов заданы в _LADDER (app.py:1308): exact→adjacent sibling→parent→widen time/distance→change format→event/room/group→saved search; _tag_ladder штампует ladder_step/axis/relaxes на исполненные fallback-карточки, online-room помечается step 6, 'save search' — step 7; покрыто тестами C12-LADDER-ORDER, C12-LADDER-INVARIANTS, C12-TAG, C12-TAG-ONLINE. — «app.py:1308 _LADDER; :1394 _tag_ladder; test_core_v2.py:1099-1133» |
| ✅ | **§12.3** Пример Dota 2 | реализовано | Таблица шагов T0/T1/T2/T4/No-supply зашита как _DOTA_EXAMPLE (app.py:1339) и возвращается в expansion_ladder(); правило 'LoL-игрок не получает personal proposal под видом близкого Dota-совпадения' реально обеспечивается outreach-гейтом _outreach_ok (adjacent/parent → discovery-only без broad consent) в _negotiate_precheck (app.py:1630). — «app.py:1339 _DOTA_EXAMPLE; :1630 _outreach_ok gate» |
| ✅ | **§13.0** Agent protocol — типизированные события вместо свободных LLM-диалогов (intro) | реализовано | Спина протокола — детерминированный LLM-free модуль shared/kleal_protocol.py (импортируется в app.py как kp); он не содержит доступа к моделям, единственный LLM-вызов negotiate_one лишь ФОРМУЛИРУЕТ решение, а accept/reject-бит маппится в типизированное действие через kp.map_decision_to_action. — «shared/kleal_protocol.py:1-20; app.py:16 import kp» |
| ✅ | **§13.1** Разрешённые действия + конверт сообщения | реализовано | Девять действий заданы кортежем kp.ACTIONS (kleal_protocol.py:13); kp.build_envelope (:60) оборачивает proposal в конверт с versions(intent/profile_capsule/policy), purpose, disclosure_scope, structured_fields, rendering_key, audit и НЕ переderiv-ит proposal_id/idempotency_key/TTL; реально вызывается в negotiate_candidates (app.py:1772); покрыто C13-ACTION и C13-ENVELOPE. — «kleal_protocol.py:13 ACTIONS, :60 build_envelope; app.py:1772; test_core_v2.py:1157-1179» |
| ✅ | **§13.2** Волны предложений | реализовано | kp.assign_wave (kleal_protocol.py:84) присваивает Wave 2 очередникам ('queued for the next wave'), Wave 3 при urgent/expansion, иначе Wave 1; параллельный cap берётся из конфига (default 2 / urgent 3) в _negotiate_precheck (app.py:1604), broadConsent поднимает cap до 3 (app.py:1606); Wave 0 'показать slate' — отдельный /match путь; массовой рассылки нет; покрыто NEG2, C13-WAVE, C13-STAMP, C13-CAP-CONSENT. — «kleal_protocol.py:84 assign_wave; app.py:1604-1608, :1671; test_core_v2.py:301,1181-1221» |
| ✅ | **§13.3** Границы автономности | реализовано | Манифест kp.AUTONOMY_BOUNDARIES (kleal_protocol.py:98) перечисляет can_auto/requires_consent; три границы ЭНФОРСЯТСЯ рабочими гардами — guard_no_payment_booking_venue_confirm, guard_no_conflicting_plans, guard_no_refusal_retry — и первые два вызываются перед матчем в negotiate_candidates (app.py:1797-1801, при срабатывании ставят code NEEDS_CONSENT/DECLINE); расширение hard-constraints и dating спрятаны за /confirm (apply_confirmation), раскрытие sensitive-поля — за disclosure-clamp; покрыто C13-GUARD-PAY/PLAN/RETRY и C13-BOUNDARIES. — «kleal_protocol.py:98 AUTONOMY_BOUNDARIES, :111-143 guards; app.py:1797-1802; test_core_v2.py:1188-1204» |

## §14 Transaction state machines и защита от гонок

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **§14.0** Proposal/Match — транзакционные сущности с повторной policy-проверкой | реализовано | Proposal и Match реализованы как транзакционные сущности: app.py:negotiate_candidates под _STORE_LOCK повторно вызывает revalidate(intent,c,'accept') перед фиксацией accept и переводит proposal SENT->ACCEPTED через ks.apply_transition (§14.4 policy_revoked_before_accept штампуется при флипе политики). — «services/matching/app.py:1785-1814; shared/kleal_states.py» |
| ✅ | **14.1** Состояния (Intent/Proposal/Match/Plan) | реализовано | Все четыре машины заданы в ks.STATE_MACHINES с точными переходами из спеки (intent DRAFT..CANCELLED, proposal CREATED..POLICY_REVOKED, match PENDING_DISCLOSURE..SAFETY_CLOSED, plan DRAFT..NO_SHOW); intent/proposal/match enabled:True, plan enabled:False (pilot-disabled); покрыто тестами C14-STATES/TRANSITIONS. — «shared/kleal_states.py:28-91; test_core_v2.py:1233-1259» |
| 🟡 | **14.2** Технические гарантии (concurrency/idempotency/outbox/CAS/trace) | частично | Реализованы и протестированы: optimistic concurrency по version (check_version/compare_and_swap, C14-CAS), idempotency-dedup (dedup/dedup_key, применён в agent_transition и /agent/outcome), unique_active_pair (C14-PAIR), reservation TTL (reservation_expired, C14-RESERVATION-TTL), policy-revalidation в той же транзакции accept (под _STORE_LOCK); но transactional outbox и immutable decision trace — это лишь синхронные списки SESSION['_outbox']/SESSION['_trace'] в рамках процесса без async-consumer/дедупликации потребителей (docstring помечает это blocked_infra), а idempotency-key покрывает не все write-эндпоинты. — «shared/kleal_states.py:153-215; services/matching/app.py:1835-1854» |
| 🟡 | **14.3** Одновременные принятия (политика по типу intent) | частично | ks.concurrent_accept_policy классифицирует все 5 строк таблицы (one_to_one_fixed_time exclusive, multiple_conversations default, dating_manual_confirm, group_reservation, event_capacity); в negotiate_candidates реально применяется только exclusive-ветка 1:1 fixed-time через ks.claim_slot (первый accept выигрывает, второй -> SLOT_TAKEN/WITHDRAW), а group/event помечены enabled:False (pilot-off §15/§16); покрыто C14-POLICY/SLOT. — «shared/kleal_states.py:236-276; services/matching/app.py:1784-1812» |
| ✅ | **14.4** Критические race cases -> детерминированный код без утечки | реализовано | ks.resolve_race маппит все 8 канонических гонок (policy_revoked/blocked/privacy/slot_taken/duplicate_pair/duplicate_replay/reservation_expired/timezone_infeasible) в детерминированный {to,error_code,public_reason,leak:False}; в живом потоке подключены policy_revoked_before_accept и slot_taken через _stamp_txn, остальные доступны детерминированно; покрыто тестом C14-RACES (leak:False у всех). — «shared/kleal_states.py:283-309; services/matching/app.py:1739-1749; test_core_v2.py:1309» |

## §15 Group Formation Core + §16 Events/rooms/venues (AS-IS)

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ⛔ | **§15.0** Группа оценивается как множество, а не среднее парных совпадений | pilot-выкл (scaffolding) | shared/kleal_groups.py реализует детерминированную множественную оценку группы (group_utility с термином least_misery = min направленной удовлетворённости), но модуль — conformance-scaffolding, отключён в пилоте (summary()['enabled']=False), не подключён в живой person-flow. — «shared/kleal_groups.py:group_utility, least_misery» |
| ⛔ | **15.1** Set-level constraints (min/max size, время, capacity, роли, blocks, host, skill spread, language, equipment, quorum) | pilot-выкл (scaffolding) | kg.check_set_constraints выдаёт сортированные коды нарушений для всех перечисленных ограничений (SIZE_MIN/MAX, QUORUM, CAPACITY_EXCEEDED, MANDATORY_ROLE_UNMET, PAIR_BLOCKED, SAFETY_EXCLUDED, HOST_MISSING, SKILL_SPREAD, LANGUAGE_UNCOVERED, EQUIPMENT_MISSING, PLATFORM_UNSUPPORTED, TIME_OVERLAP_INSUFFICIENT); тесты C15G-18/19 покрывают LANGUAGE/SKILL/HOST/EQUIPMENT/time; scaffolding отключён в пилоте. — «shared/kleal_groups.py:326 check_set_constraints; test C15G-18/19» |
| ⛔ | **15.2** Group utility = 0.35·least_misery + 0.25·mean_pair_fit + 0.20·role_coverage + 0.10·time_overlap + 0.10·diversity_value; feasibility доминирует | pilot-выкл (scaffolding) | kg.group_utility_components считает все 5 компонент в [0,1] с весами, читаемыми ТОЛЬКО из sha-pinned config-блока group_formation (yaml:220-229, keys least_misery/mean_member_relevance/role_coverage/time_overlap/diversity_budget), а group_utility возвращает utility=None при любом hard-нарушении; тесты C15G-01..04/14; отключено в пилоте. — «shared/kleal_groups.py:288 group_utility_components, :406 group_utility; config yaml:220» |
| ⛔ | **15.3** MVP algorithm: seed → feasible pools → greedy marginal → local repair → reservations → invitations → waitlist | pilot-выкл (scaffolding) | kg.form_group реализует все 7 шагов (filter_hard_set_constraints, generate_role_complete_seeds, greedy_marginal_add, local_repair, reserve_members через ks.claim_slot, build_group_invitations конструирует но НЕ шлёт, replacement_policy waitlist/cancel-or-reform); тесты C15G-05..10/16/17/20; тело недостижимо без точного override enable_group_formation, результат всегда enabled:False. — «shared/kleal_groups.py:676 form_group, :701 run_group_formation; app.py:2172 /api/agent/group» |
| 🟡 | **15.4** Примеры доменов: Dota stack / падель / разговорная группа / прогулка | частично | Обобщённые примитивы (mandatory/optional roles, level band, max_skill_spread, required_equipment/platform, required_languages, size/quorum, time windows) выражают все 4 домена, и Dota-стек с покрытием carry/support/mid/offlane реально форматируется в тестах C15G-16; конкретных доменных паков для падель/разговор/прогулка в коде нет (docstring: domain packs deferred). — «shared/kleal_groups.py:21 (deferred); test C15G-16 _DOTA/_DOTA_C» |
| ⛔ | **§16.0** Events/rooms/venues — отдельные типы кандидатов, не «пользователь с большой capacity» | pilot-выкл (scaffolding) | shared/kleal_candidates.py задаёт event/room/venue как отдельные объекты (build_event/room/venue) со штампом is_personal_match:False, is_alternative:True, tier T4, closes_intent_as:'alternative' на этапе сборки; направленный user→object скоринг никогда не читает обратные/reciprocal поля (тест C16-EVENT-NOT-USER/C16-DIRECTED-ONEWAY); модуль отключён в пилоте. — «shared/kleal_candidates.py:216 _intrinsic; test C16-EVENT-NOT-USER» |
| ✅ | **§16.User** User: mutual policy+directed fit → reciprocal relevance → proposal→mutual contact | реализовано | Строка User — это живой person-to-person пилот-поток (core_v2 reciprocal scoring + app.py match/negotiate/proposal), а НЕ kleal_candidates; kleal_candidates намеренно не содержит reciprocal_score/mutual policy (тест C16-BYTE-IDENTICAL-PERSON-SLATE подтверждает изоляцию). — «services/matching/app.py match/negotiate; test C16-BYTE-IDENTICAL-PERSON-SLATE» |
| ⛔ | **§16.Ad-hoc group** Ad-hoc group: set feasibility+quorum → group utility → reservations→group confirmation | pilot-выкл (scaffolding) | Реализовано в §15 kleal_groups (set_feasible+quorum-гейт, group_utility ранжирование, reserve_members создаёт reservations через kc.build_reservation); отключено в пилоте, только под override. — «shared/kleal_groups.py:623 reserve_members» |
| ⛔ | **§16.Event** Event: category/schedule/capacity/access eligibility → user→event relevance → registration/external handoff | pilot-выкл (scaffolding) | kct.event_eligibility (CATEGORY_MISMATCH/SCHEDULE_PAST/SCHEDULE_NO_OVERLAP/CAPACITY_FULL/ACCESS_AGE/VERIFIED/LANGUAGE/PAID/GEO), user_event_relevance с EVENT_WEIGHTS, event_transaction (external_handoff при url иначе seat registration+waitlist); тесты C16-ELIG-EVENT/CAPACITY-EVENT; отключено в пилоте. — «shared/kleal_candidates.py:290 event_eligibility, :482 event_transaction» |
| ⛔ | **§16.Online room** Online room: platform/topic/live capacity/moderation → session relevance → join token/waitlist | pilot-выкл (scaffolding) | kct.room_eligibility (TOPIC_MISMATCH/PLATFORM_UNSUPPORTED/MODE_OFFLINE_NO_CONSENT/LIVE_CAPACITY_FULL/MODERATION_REQUIRED/LANGUAGE), session_relevance с ROOM_WEIGHTS и _healthy_liveness, room_transaction выдаёт join_token или waitlist (capacity=max_live-current); тесты C16-ELIG-ROOM/CAPACITY-ROOM; отключено в пилоте. — «shared/kleal_candidates.py:323 room_eligibility, :507 room_transaction» |
| ⛔ | **§16.Venue** Venue: availability/price/noise/distance/accessibility → plan suitability → selection/booking handoff | pilot-выкл (scaffolding) | kct.venue_eligibility (UNAVAILABLE/PRICE_OVER_BUDGET/GEO/NOISE_UNACCEPTABLE/ACCESSIBILITY_REQUIRED), plan_suitability с VENUE_WEIGHTS, venue_transaction возвращает selection (kc.build_plan) либо booking_handoff; enum 'venue' добавлен аддитивно (intent_to_venue:False); тесты C16-ELIG-VENUE/VENUE-ENUM-ADDITIVE; отключено в пилоте. — «shared/kleal_candidates.py:347 venue_eligibility, :530 venue_transaction; app.py:554» |
| ⛔ | **§16.alt** Event/venue может закрыть intent как честная АЛЬТЕРНАТИВА, а не «совпадение с людьми» | pilot-выкл (scaffolding) | kct.rank_candidates/run_candidates выдают слейт из T4-карт с is_personal_match:False/is_alternative:True/closes_intent_as:'alternative', закрывающих intent без персонального матча; тест C16-CLOSES-INTENT-WITHOUT-PERSON подтверждает непустой альтернативный слейт при enabled:False; отключено в пилоте. — «shared/kleal_candidates.py:449 rank_candidates, :567 run_candidates; test C16-CLOSES-INTENT-WITHOUT-PERSON» |

## §17 Dating — отдельный режим продукта

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| 🟡 | **17.0** Dating — не ещё один набор весов поверх friendship | частично | Dating выделен в отдельный домен: infer_domain изолирует его (core_v2.py:148), у него собственный weights-блок + release_gate в конфиге (config/Kleal_Matching_Core_Config_v2.yaml:171-185), отдельный hard-gate datingOk и purpose-bound profile view; но реализация — это по сути отдельный weight-блок + гейты, а не отдельная capsule/контур данных. — «core_v2.py:infer_domain:148; config yaml:171» |
| 🟡 | **17.1.consent** Consent: отдельное включение режима + явное подтверждение target preferences | частично | Переход в dating требует явного подтверждения: ki.clarification выставляет P0-gap dating_opt_in (kleal_intent.py:274), а ki/kc.apply_confirmation промотит proposedType->type=dating только после подтверждения (kleal_intent.py:211-223); отдельного toggle-режима и записи о подтверждённых target preferences нет. — «shared/kleal_intent.py:274, 211-223» |
| 🟡 | **17.1.profile** Profile: отдельная dating capsule; purpose-bound данные | частично | Purpose-binding реализован и протестирован: build_profile_view отдаёт для dating поля без 'entities', а для friendship/networking/etc — без 'datingOk' (kleal_contracts.py:83-87, тесты C4-PV1..PV4), инвариант проверяется _selfcheck; но отдельной dating-capsule как хранилища нет — есть один флаг datingOk. — «shared/kleal_contracts.py:87,512-516; test C4-PV» |
| ✅ | **17.1.age** Age: строгие legal/age gates, несовершеннолетние исключены | реализовано | Жёсткий 18+ floor: _hard_gates возвращает 'under 18' для age<MIN_AGE(18) до скоринга (app.py:212,709), кандидат-несовершеннолетний никогда не скорится (тест C5b), а dating-intent по умолчанию ужесточает minAge=18/verifiedOnly (build фолбэк app.py:641). — «services/matching/app.py:709,212; test C5b» |
| ⬜ | **17.1.orientation** Orientation/preferences: чувствительный контур с минимизацией данных | не реализовано | Поля orientation/sexual-preferences в модели нет вообще — хранится лишь булев datingOk; отдельного чувствительного контура с минимизацией/специальной обработкой не реализовано (минимизация достигается только исключением datingOk из не-dating views). — «shared/kleal_contracts.py:87» |
| 🟡 | **17.1.disclosure** Disclosure: staged profile disclosure (фото/имя/детали по настройкам) | частично | Есть общий staged-disclosure слой: DISCLOSURE_STAGES (minimal/limited_profile/match_only/full), build_profile_view принимает disclosure_stage, а build_proposal клампит до min(intent, candidate.receiving.stage) (kleal_contracts.py:53,171,405-416); он не dating-специфичен и не привязан к отдельным настройкам фото/имени. — «shared/kleal_contracts.py:53,405-416» |
| ✅ | **17.1.outreach** Outreach: каждый proposal требует явного действия; no auto-accept | реализовано | Для romantic/dating commit_policy=dating_manual_confirm с auto_commit=False (kleal_states.py:249-251); REVIEW-кандидат принудительно non-proposable (_allocation_action->review_required, app.py:766), а send-boundary заново прогоняет гейты (тесты NEG1-4). — «shared/kleal_states.py:250; app.py:766; test NEG» |
| 🟡 | **17.1.explanation** Explanation: не выводить чувствительные причины и псевдопсихологический score | частично | Профиль показывает качественную match-band, а не сырой процент совместимости (коммит §9.7, fcdc31b), а приватные BLOCK-причины (напр. 'not open to dating') остаются только в owner-панели explain и не уходят наружу (kleal_states.py:282); отдельного фильтра чувствительных dating-причин в объяснениях нет. — «shared/kleal_states.py:282; commit fcdc31b §9.7» |
| 🟡 | **17.1.safety** Safety: block/report, rate limits, anti-harassment, private-location safeguards | частично | Частично: есть block-состояние ребра (EDGE_STATES 'block', kleal_contracts.py:60,479) и rate-ограничения (COOLDOWN_DAYS, MAX_PENDING, receiving per_7d cap), локация огрублена (coarse_cell/km-банды, не точный адрес); отдельного report-флоу и anti-harassment-механик нет. — «shared/kleal_contracts.py:60,479; app.py:210-211» |
| 🟡 | **17.1.audit** Audit: отдельные policy version, retention и access controls | частично | Snapshot несёт policy_version ('policy-2.0.0') и per-domain retention TTL (dating=14 дней, kleal_contracts.py:101; app.py:601), но версия политики единая (не отдельная для dating), а отдельных access controls нет. — «services/matching/app.py:601; shared/kleal_contracts.py:101» |
| 🟡 | **17.2** Изоляция от других режимов | частично | Изоляция обеспечена: infer_domain отделяет dating (core_v2.py:148), _cross_purpose_blocked/_TYPE_PURPOSE блокирует авто-кросс dating<->networking/language/sport и читает purpose только из собственных intent кандидата (app.py:671-710), а переход friendship->dating требует нового подтверждения через apply_confirmation; требование «отказ в dating не влияет на friendship ranking» отдельно кодом не выражено. — «services/matching/app.py:671-710; core_v2.py:148» |
| ⛔ | **17.3** Gate для пилота (отдельные UX/safety/moderation/DPIA/legal/abuse) | pilot-выкл (scaffolding) | release_gate=separate_safety_legal_track из конфига принуждается в _policy_decision: неверифицированный dating-кандидат становится REVIEW (discoverable, но non-proposable — держится до отдельного safety/legal трека), а release_status=pilot_candidate (app.py:735-759,548; тесты REVIEW1/REVIEW2); полноценных отдельных UX/moderation/DPIA/incident-процессов нет — это conformance-гейт, а не готовый пилот. — «services/matching/app.py:758-759; config yaml:172; test REVIEW1/2» |

## §18 Доменные сценарии и декомпозиция intent

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| 🟡 | **§18.0** Раздел: доменная декомпозиция intent | частично | Декомпозиция по доменам реализована через core_v2.infer_domain (10 доменов) + доменные веса в config и слоты kc.domain_details (platform/server/rank/level/ticket/equipment), но доменно-специфичные hard-слоты (server/capacity/ticket) для персон в скоринге не гейтятся, а часть доменов (coworking) не имеет собственного признака. — «core_v2.py:146 infer_domain; kleal_contracts.py:312 domain_details; config/Kleal_Matching_Core_Config_v2.yaml:44 domains» |
| 🟡 | **§18.1** Обзор доменов (таблица slots/hard/fallback) | частично | Все 10 доменов таблицы присутствуют (social_meet/walk/games/language_exchange/sport_activity/culture_event/professional_networking/watch_together/coworking/dating) с блоками весов в config и слотами в domain_details, но 'типичный fallback' един и не доменно-специфичен — это один общий ladder expansion_ladder (app.py:1347), а hard-примеры (age mode, mutual visibility, platform/server, ticket, capacity) гейтятся лишь частично. — «config/...v2.yaml:44-181; core_v2.py:140 _TYPE2DOMAIN/_BROAD2DOMAIN» |
| 🟡 | **§18.2** Сценарий Dota 2 (games) | частично | domain=games выводится по типу/топикам, роли self_role=carry/role_needed=support моделируются признаком directed_preferences (core_v2.py:260, match 0.55 / conflict 0.15), 'спокойный' идёт как soft-vibe (social_context), а тир-строки Dota (T0..T4 + No-supply) заданы в app._DOTA_EXAMPLE и покрыты тестом C12-DOTA; при этом ranked-режим и server 'EU West' для персон в hard-гейт не превращаются (platform-гейт есть только для rooms — kct.room_eligibility PLATFORM_UNSUPPORTED), а нехватка platform выдаёт P0-уточнение platform_crossplay (тест C5-CLAR2). — «core_v2.py:260; app.py:1339 _DOTA_EXAMPLE; test_core_v2.py:1116 C12-DOTA, :653 C5-CLAR2» |
| 🟡 | **§18.3** Сценарий прогулки по Барселоне (walk) | частично | domain=walk определяется по ключевым словам (core_v2.py:152), взрослый режим гейтится minAge, зона — soft geo-бэндами (GEO_BANDS), приватность/'рядом сейчас без receiving policy не предлагаем' обеспечивается passive_outreach_default=off_unless_receiving + консервативным reverse-покрытием, но overlapping window и min-duration/accessibility маршрута моделируются лишь как soft time_feasibility, а не как отдельные walk-специфичные hard-условия. — «core_v2.py:152,172 GEO_BANDS,222 time_feasibility; config:219 passive_outreach_default» |
| 🟡 | **§18.4** Сценарий практики испанского (language exchange) | частично | domain=language_exchange с доменным признаком domain_constraints, сверяющим желаемый язык топика с языками кандидата (core_v2.py:289, K_MATCH/K_MISM), а комплементарность learner↔native выражена типизированной матрицей kt.complementary (тест C6-COMPL1) — но она только метаданные карточки и в скор НЕ подаётся (comment 'never fed into scoring'), т.е. exchange-value/directed roles не влияют на ранжирование напрямую. — «core_v2.py:289; kleal_taxonomy.py:256 _COMPLEMENT,:268 complementary; test_core_v2.py:696 C6-COMPL1» |
| 🟡 | **§18.5** Сценарий профессионального кофе (networking) | частично | domain=professional_networking с доменными весами; язык гейтится requiredLanguages, гео — soft, роль — directed_preferences, а 'открыт ли B к профилю A' обеспечивается reverse_features + receiving-policy/reciprocal; industry/goal выдаются как domain-critical уточнения (kleal_intent._domain_critical), но профессиональная цель/founder-роль как hard-critical гейт не реализованы, а adjacent-roles fallback покрыт лишь общим ladder. — «core_v2.py:315 reverse_features; kleal_intent.py:38 professional_networking->[industry,goal]; config:129» |
| 🟡 | **§18.6** Краткие сценарии (падель/выставка/футбол/коворкинг/встреча/dating-прогулка) | частично | Разнородно: падель-capacity/group-reservation и типы Event/Room/Venue (выставка, футбольный fan-group/venue) реализованы как детерминированное scaffolding в kleal_groups/kleal_candidates, но PILOT-DISABLED (enabled:False, за override-гейтом); коворкинг признан доменом с весами, но коворкинг-специфика (тишина/часы/venue) в скоринге НЕ считается — build_features для coworking отдаёт domain_constraints=NA; 'обычная встреча' с calm-coffee fallback идёт через общий expansion; а dating-изоляция реализована и надёжна — infer_domain даёт dating только при type=='dating', dating-намерение в friendship требует явного подтверждения (kleal_intent evergreen/dating_opt_in P0), т.е. 'погулять вечером' не становится романтическим неявно. — «kleal_candidates.py:1 (pilot-disabled); core_v2.py:299-306 (coworking->NA); core_v2.py:148 infer_domain; kleal_intent.py:172,274 dating_opt_in» |

## §19 Feedback/learning loop + §20 Метрики (Kleal Matching Core v2)

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| 🟡 | **§19.0** Feedback, learning loop и память (интро) | частично | Есть файловый outcome-лог (SESSION['_outcomes']) и demote-only цикл обратной связи по явным решениям владельца, но полноценного «обучения по исходам» и механизма пользовательского контроля памяти нет; record_feedback (app.py:339) лишь хранит per-owner accepted/rejected. — «services/matching/app.py:315-389 (_log_exposure/_record_outcome/_success_metrics), record_feedback:339» |
| 🟡 | **19.1** Outcome taxonomy | частично | Реализованы стадии exposure (_log_exposure), proposal (proposed/accepted/declined/expired_no_response) и completion (completed), а primary-positive = Completed Positive Interaction считается в _success_metrics (completed_ts + двусторонний accept, silence_is_negative=False); стадии consideration/coordination/quality/safety и сигналы comfort/usefulness/would_repeat/no-show не собираются (kleal_completion.completion_factors помечает recent_no_show/response_latency как not_collected). — «app.py:350 _OUTCOME_STAGES, app.py:377 _success_metrics; shared/kleal_completion.py:60-65 not_collected» |
| 🟡 | **19.2** Правила обновления профиля | частично | Модель данных несёт §4.2 иерархию источников (explicit_stable_profile L4 перекрывает agent_inference L7), а Evidence имеет confidence/source/allowed_purposes и подтверждение чувствительных ограничений (ki.apply_confirmation); но decay и deletion path для inferred-сигналов в коде отсутствуют, feedback хранится глобально per-name (не привязан к домену), поэтому изоляция dating-feedback от prof/social ранжирования не реализована. — «shared/kleal_contracts.py:22-31 SOURCE_HIERARCHY, build_evidence:359; shared/kleal_intent.py:209 apply_confirmation; app.py:339 record_feedback (global)» |
| 🟡 | **19.3** Защита от feedback bias | частично | Корректно реализовано отделение «не увидел» от «отказал» и нейтральность timeout: expired_no_response — отдельная стадия, silence_is_negative=False, не подаётся в понижающий цикл (app.py:346-389); есть exposure-лог и позиция в allocation_trace, а также shadow_replay для safe-проверки правок таксономии (kleal_taxonomy.py:373, тесты C6-SHADOW); но exploration отключён (exploration_quota=0), propensity не логируется (propensity_status="absent_by_design"), а отдельная проверка safety/fairness не реализована. — «app.py:347,387 (silence neutral), app.py:1002 exploration_quota=0, app.py:1097 propensity absent_by_design; shared/kleal_taxonomy.py:373 shadow_replay» |
| 🟡 | **§20.0** Метрики, эксперименты и критерии качества (интро) | частично | Единственная рантайм-метрика — /api/agent/outcomes (_success_metrics: stages/matches/completed_interactions), системы показателей/экспериментов/критериев качества как таковой нет. — «app.py:2057 /api/agent/outcomes, app.py:377 _success_metrics» |
| ⬜ | **20.1** Система показателей (12 контуров) | не реализовано | Системы обязательных показателей по 12 контурам нет; из перечисленного присутствуют лишь единичные фрагменты — success-метрики Completion (_success_metrics) и allocation_trace c позицией/exploration/propensity как observability, но метрики slot accuracy/recall T0-T2/monotonicity/fairness exposure/economics в рантайме не считаются. — «app.py:377 _success_metrics, app.py:1078-1097 allocation_trace» |
| 🟡 | **20.2** Staged validation (7 шагов) | частично | Полностью выполнен только шаг 2 (Unit/property tests): test_core_v2.py — 264 приёмочных теста на gates/weights/monotonicity/unknown/state-transitions (C1-C24, R1-7, RCV1-9, NEG1-4); шаг 1 (500-1000 размеченных кейсов), шаг 3 (полный shadow-mode отправки), шаги 4-7 (dogfood/Barcelona pilot/controlled expansion/ML migration) в коде не реализованы, хотя pilot-gating для групп/событий/dating присутствует как заглушки. — «services/matching/test_core_v2.py (264 checks); shadow_replay ограничен правками таксономии (kleal_taxonomy.py:373)» |
| ⬜ | **20.3** Guardrails | не реализовано | Фреймворка экспериментов с primary outcome / harm guardrails / segment breakdown / stop-continue нет; единственное родственное — конструктивная нейтральность silence в success-метрике, но правило «не объявлять улучшением рост при росте no-show/reports/fatigue/concentration» не кодифицировано. — «app.py:377-389 _success_metrics (только silence_is_negative=False)» |

## §21 Техническая архитектура, API и observability (AS-IS)

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| ✅ | **21.1** Компоненты — Intent Compiler | реализовано | kleal_intent.validate_and_normalize + app.parse_intent превращают free-text в canonical draft (16 ключей, allowlist type/role/mode, clamp), LLM — недоверенный парсер с _fallback_parse; clarification даёт _section5_addendum. — «shared/kleal_intent.py; services/matching/app.py:parse_intent(644)» |
| 🟡 | **21.1** Компоненты — Profile/Consent Service | частично | Отдельного сервиса нет: профиль строит onboarding, consent частично выражен флагами intent (verifiedOnly, confirmed constraints §5.1) и purpose-binding; deletion и profile-versions в snapshot не реализованы (profile_versions отсутствует). — «services/onboarding/app.py; services/matching/app.py:_request_snapshot(593)» |
| 🟡 | **21.1** Компоненты — Match Capsule Builder | частично | kc.build_match + app._match_capsules материализуют purpose-bound капсулы из SESSION['_matches'] после взаимного accept, но compact purpose-minimised representation ограничена (без полного disclosure-профиля). — «services/matching/app.py:_match_capsules(367); shared/kleal_contracts.py:build_match» |
| ✅ | **21.1** Компоненты — Taxonomy Registry | реализовано | TAXONOMY/SYNONYMS/ADJACENCY + read-only narrator kleal_taxonomy дают concepts/edges/distances; forbidden expansions обеспечены консервативной ADJACENCY (§6). — «services/matching/app.py:32-203; shared/kleal_taxonomy.py» |
| 🟡 | **21.1** Компоненты — Candidate Retrieval | частично | load_candidates читает shared store (fallback demo-pool 50) + RETRIEVAL_STAGES описывает 7 стадий, но SQL/geo/PostGIS/H3 и pgvector ANN помечены blocked_infra — работает только in-memory structured retrieval. — «services/matching/app.py:load_candidates(476), RETRIEVAL_STAGES(1110)» |
| ✅ | **21.1** Компоненты — Policy Engine | реализовано | _policy_decision возвращает три-state ALLOW/BLOCK/REVIEW с disclosure (reason/why_no_outreach), включая cross-purpose isolation и release_gate для dating. — «services/matching/app.py:_policy_decision(735)» |
| 🔒 | **21.1** Компоненты — Feature Builder | в движке (sha-pinned) | core_v2.build_features строит 7 канонических evidence-групп с состояниями known/unknown/NA (applicability) и coverage; живёт в sha-пиннутом движке, не редактировался. — «services/matching/core_v2.py:build_features (PINNED_SHA 21505ccb)» |
| 🔒 | **21.1** Компоненты — Relevance Engine | в движке (sha-pinned) | core_v2.directional_score даёт directional mean/coverage/lcb (R_lcb с λ-штрафом за неопределённость); в sha-пиннутом ядре. — «services/matching/core_v2.py:directional_score» |
| 🔒 | **21.1** Компоненты — Reciprocity/Readiness | в движке (sha-pinned) | core_v2.reciprocal_score (reverse fit) + readiness_state (open_now/busy/quiet/paused) реализованы в sha-пиннутом ядре. — «services/matching/core_v2.py:reciprocal_score,readiness_state» |
| 🟡 | **21.1** Компоненты — Allocation Engine | частично | _slate diversify даёт diversity (≤3/bucket) и caps (top_n); exploration, fairness, popularity/responsive-caps присутствуют как механизмы, но выключены (пороги 10**9), а _stamp_allocation_trace добавляет read-only reasons. — «services/matching/app.py:_slate,_alloc_cfg(1000),_stamp_allocation_trace(1076)» |
| 🟡 | **21.1** Компоненты — Match Orchestrator | частично | app.py оркестрирует match→negotiate→outcome и waves через kp, но persistent search run/reservations нет (кросс-request proposal-store помечен blocked_infra, объект round-trip'ится вызывающим). — «services/matching/app.py:do_POST(2076); kleal_protocol.py» |
| ⛔ | **21.1** Компоненты — Group Formation Core | pilot-выкл (scaffolding) | kleal_groups.run_group_formation реализует set-level feasible groups, но эндпоинт /api/agent/group отдаёт enabled:False без точного override {'enable_group_formation':True}. — «services/matching/app.py:2172; shared/kleal_groups.py» |
| 🟡 | **21.1** Компоненты — Plan Coordinator | частично | kleal_states даёт plan-state machine с transitions/CAS, agent_plan собирает план из query, но time/place/room-координация минимальна (нет отдельного plan-store). — «shared/kleal_states.py; services/matching/app.py:agent_plan(1481)» |
| 🟡 | **21.1** Компоненты — Feedback & Learning | частично | Feedback-loop (record_feedback ±) + outcome-события (_record_outcome, _matches, _success_metrics) реализованы в kleal_store.json; training sets и memory suggestions не реализованы. — «services/matching/app.py:record_feedback(339),_record_outcome(351),_success_metrics(377)» |
| ⬜ | **21.1** Компоненты — Cost Governor | не реализовано | llm-service держит URL/ключи моделей, но component-уровня cost governor (model routing по бюджету, cache, budgets) в matching нет. — «services/llm/app.py» |
| 🟡 | **21.1** Компоненты — Audit/Observability | частично | Есть per-candidate decision-trace карточки, immutable _trace + _outbox на переходах, allocation_trace; replay/SLA-метрики/incident-data как подсистемы не реализованы. — «services/matching/app.py:agent_transition(1849), explain_match(2014)» |
| 🟡 | **21.2** API — POST /intents/compile | частично | Функциональный эквивалент /api/agent/plan (LLM-parse) и /api/agent/match; отдельного /intents/compile нет, draft/slots/confidence/clarification отдаются как §5-addendum, а не как один объект. — «services/matching/app.py:2079,2088» |
| 🟡 | **21.2** API — POST /intents/{id}/confirm | частично | /api/agent/confirm применяет подтверждённые sensitive-constraints через ki.apply_confirmation и рематчит; intent-version как персистентный ресурс не хранится, consents фиксируются только в объекте. — «services/matching/app.py:2113» |
| 🟡 | **21.2** API — POST /searches (idempotency key) | частично | /api/agent/match выполняет search синхронно и возвращает snapshot+intent_version, но это не async search run: нет search_id-ресурса и idempotency-key на поиске. — «services/matching/app.py:2088» |
| ⬜ | **21.2** API — GET /searches/{id} | не реализовано | Ресурса search с state/transparent slate по id нет — поиск синхронный, результат возвращается сразу в ответе на match. — «services/matching/app.py:do_GET(2034)» |
| 🟡 | **21.2** API — POST /searches/{id}/expand | частично | expansion_ladder возвращается инлайн в ответе match/confirm (§12 план осей), но отдельного эндпоинта разрешения конкретной оси расширения нет. — «services/matching/app.py:2105; expansion_ladder» |
| 🟡 | **21.2** API — POST /proposals | частично | Создание proposal покрыто /api/agent/negotiate (+ _log_proposal, outcome 'proposed'), но reservation-store как ресурса нет — proposal-объект round-trip'ится вызывающим. — «services/matching/app.py:2248,_log_proposal(299)» |
| 🟡 | **21.2** API — POST /proposals/{id}/respond | частично | /api/agent/transition обрабатывает accept/decline/counter/withdraw через kleal_states с CAS и идемпотентным replay, но без отдельного proposal-id-ресурса (id внутри object). — «services/matching/app.py:2165,agent_transition(1823)» |
| ⛔ | **21.2** API — POST /groups/form | pilot-выкл (scaffolding) | /api/agent/group вызывает kg.run_group_formation, но по умолчанию dormant (enabled:False) без точного override. — «services/matching/app.py:2172» |
| 🟡 | **21.2** API — POST /plans (version check) | частично | Отдельного /plans нет; план-состояния меняются через /api/agent/transition (entity=plan) с expected_version/CAS, самого plan-CRUD-ресурса нет. — «services/matching/app.py:2165; shared/kleal_states.py» |
| ✅ | **21.2** API — POST /interactions/{id}/feedback | реализовано | /api/agent/feedback (accept/reject) и /api/agent/outcome (proposed/accepted/declined/completed/expired) записывают structured outcomes с идемпотентностью по idempotency_key. — «services/matching/app.py:2145,2148» |
| 🟡 | **21.2** EVENT policy_changed | частично | /api/agent/revalidate ре-решает пару на живом сторе и отдаёт code=POLICY_CHANGED если строже (checkpoints #3-#7), но это pull-эндпоинт, не event-bus с подпиской/revocation. — «services/matching/app.py:2227,revalidate(1712)» |
| 🟡 | **21.2** EVENT match_state_changed | частично | agent_transition при успехе аппендит immutable _trace {entity,from,to,version,action,id} — аудируемый transition, но без реального event-эмиттера/шины. — «services/matching/app.py:1849» |
| 🟡 | **21.2** EVENT exposure_logged | частично | _log_exposure пишет per-name impression-лог на send-boundary и allocation_trace несёт exposure_count/position — ranking/allocation trace есть, но как внутренний лог, не событийный поток. — «services/matching/app.py:_log_exposure(315),_stamp_allocation_trace(1076)» |
| 🟡 | **21.3** Decision trace | частично | Карточка match/explain несёт semantic_tier, evidence[] (group/state/value), directional a_to_b/b_to_a (mean/coverage/lcb), reciprocal, readiness, allocation (position/exploration), reason keys, policy.decision, config_version/sha из snapshot; отсутствуют как единый объект поля search_id, candidate_id (используется name), purpose_id, profile_versions, policy.version, model_versions. — «services/matching/app.py:explain_match(1987-2001),_request_snapshot(593)» |
| ✅ | **21.4** SLA — LLM compiler unavailable | реализовано | parse_intent при ошибке LLM падает на _fallback_parse (template/parser) и всё проходит ki.validate_and_normalize, критические слоты не угадываются. — «services/matching/app.py:parse_intent(644),_fallback_parse(616)» |
| ✅ | **21.4** SLA — vector index unavailable | реализовано | Векторного индекса нет вовсе (ann_recall помечен blocked_infra), ретрив всегда structured-only — деградация выполнена по построению. — «services/matching/app.py:RETRIEVAL_STAGES(1113)» |
| ⬜ | **21.4** SLA — policy service unavailable | не реализовано | Policy Engine работает in-process, отдельного policy-сервиса нет, поэтому fail-closed-для-outreach/cached-safe-for-passive путь при недоступности не реализован. — «services/matching/app.py:_policy_decision(735)» |
| 🟡 | **21.4** SLA — stale profile/capacity | частично | _negotiate_precheck/revalidate на send-boundary ре-валидируют пару против живого стора и при POLICY_CHANGED не создают proposal (accept отменяется), но capacity-revalidate ограничен (нет cross-request capacity-store). — «services/matching/app.py:_negotiate_precheck(1591),revalidate(1712)» |
| 🟡 | **21.4** SLA — notification failure | частично | agent_transition при успехе аппендит _outbox-строку и НЕ откатывает state (соответствует «state не откатывать»), но реального outbox-retry-воркера нет. — «services/matching/app.py:1851» |
| ⬜ | **21.4** SLA — ranking timeout | не реализовано | Скоринг синхронный без таймаутов; partial-transparent-results/waiting-state при таймауте ранжирования не реализованы. — «services/matching/app.py:match_candidates(1203)» |
| 🟡 | **21.4** SLA — taxonomy/config mismatch | частично | При отсутствии/тампере sha-пиннутого конфига CORE_V2 отключается и работает legacy-скорер (fail-safe), плюс kg.validate_group_config_block не-raising репортит дрейф; поведение «stop search run и alert» спеки не реализовано — вместо стопа идёт откат на legacy. — «services/matching/app.py:501-514» |
| ⬜ | **21.4** SLA — overload | не реализовано | ThreadingHTTPServer без очереди/cap concurrent runs/background search и без ступенчатой деградации объяснений — overload-политика не реализована. — «services/matching/app.py:2262» |
| 🟡 | **21.4** Цель пилота — async search / <2s ack / реальный прогресс | частично | Стадии ретрива реальны (RETRIEVAL_STAGES) и прогресс не декоративный процент, а качественный band (§9.7, коммит fcdc31b); при этом async-поиск и initial acknowledged state <2s не реализованы — /api/agent/match отвечает синхронно. — «services/matching/app.py:2088; RETRIEVAL_STAGES(1110)» |

## §22 План пилота + §23 Контракт реализации + §24 Самооценка

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| 🟡 | **22.1** Рекомендуемый scope доменов пилота | частично | Домены walk/social_meet/language_exchange/professional_networking/games заведены в canonical YAML и распознаются core_v2.infer_domain; dating изолирован под release_gate. Это конфиг/классификатор, реального supply-пилота и партиционирования нет. — «services/matching/core_v2.py:infer_domain; config/Kleal_Matching_Core_Config_v2.yaml» |
| 🟡 | **22.2.0** Этап 0 — Contracts (schemas, config registry, state machines, policy matrix) | частично | Есть config/schema.json (draft-07) + sha-pinned YAML + валидатор core_v2.load_config + матрица semantic_tiers; state machines реализованы отдельно в kleal_states.py. Единого contracts/*.ts-реестра нет; determinism-тест выступает replayable-примером. — «services/matching/core_v2.py:load_config; config/schema.json; shared/kleal_states.py» |
| ✅ | **22.2.1** Этап 1 — Rule-based core (compiler, retrieval, gates, directional relevance) | реализовано | parse_intent (LLM+fallback), load_candidates, _hard_gates и детерминированный directional_score присутствуют; offline acceptance-набор в test_core_v2.py проходит. — «services/matching/app.py; services/matching/core_v2.py; services/matching/test_core_v2.py» |
| ✅ | **22.2.2** Этап 2 — Transparent discovery (slate, reasons, list/map, no personal outreach) | реализовано | _slate выдаёт диверсифицированный список с reasons и качественными bands; explore_plans отдаёт карту реальных планов с coarse-координатами; personal outreach отделён флагом can_outreach. — «services/matching/app.py:explore_plans; services/matching/core_v2.py:_slate» |
| 🟡 | **22.2.3** Этап 3 — Controlled proposals (waves, receiving policy, idempotency, revalidation) | частично | _negotiate_precheck реализует волны (cap 2/3 параллельных), receiving-policy и ре-валидацию (ре-резолв из стора + повторные гейты); idempotency-ключ для write-flow endpoint не применяется, живого race-теста на HTTP-слое нет. — «services/matching/app.py:_negotiate_precheck» |
| 🟡 | **22.2.4** Этап 4 — Plans/outcomes (plan states, feedback, completion) | частично | record_feedback пишет accepted/rejected в файловый стор; kleal_completion даёт transparent completion_factors, Plan-автомат определён в kleal_states но enabled:False (pilot-off); end-to-end проводки plan-states нет. — «services/matching/app.py:record_feedback; shared/kleal_completion.py; shared/kleal_states.py» |
| ⬜ | **22.2.5** Этап 5 — Closed Barcelona pilot (limited areas/domains, ops dashboard) | не реализовано | Ни партиционирования городов/районов, ни ops-dashboard, ни ограничений закрытого пилота в коде нет — это pre-launch процесс, не реализованный код. |
| ⛔ | **22.2.6** Этап 6 — Group formation (reservations, quorum, replacements) | pilot-выкл (scaffolding) | kleal_groups реализует set-constraints/GroupUtility/form_group MVP с quorum-логикой как конформанс-scaffolding, но групповые decision-типы pilot-disabled в PILOT_DECISION_TYPES и не проведены в живой поток. — «shared/kleal_groups.py» |
| ⬜ | **22.2.7** Этап 7 — Calibrated ML (response/accept/completion models) | не реализовано | kleal_ml_boundary даёт только детерминированный интерфейс-заглушку без калиброванных вероятностей; никаких обученных response/accept/completion моделей нет (конфиг честно помечает acceptance_probability absent in MVP). — «shared/kleal_ml_boundary.py» |
| 🟡 | **22.3** Операционная модель пилота (partition, кап intent, ручной review, daily dashboard, incident owner, weekly review, no mass outreach, короткий feedback) | частично | Из восьми пунктов в коде реализованы только два: нет автоматического массового outreach (negotiate берёт top-5, cap 2/3 в precheck) и явный короткий feedback без анкеты (record_feedback принимает лишь accept/reject); partition, кап активных intent, ручной review, daily dashboard, incident owner, weekly review отсутствуют. — «services/matching/app.py:_negotiate_precheck; services/matching/app.py:record_feedback» |
| 🟡 | **23.1** Обязательная модульная структура (matching-core/ contracts/config/…/tests) | частично | Присутствуют config/ (yaml+schema.json+валидатор в load_config) и tests (единый test_core_v2.py); логические модули разложены по shared/kleal_*.py (contracts/intent/taxonomy/…/candidates) + app.py+core_v2.py, но нет предписанного дерева matching-core/ с contracts/*.ts и по-модульными директориями (retrieval/, policy-engine/ и т.д.). — «shared/kleal_contracts.py; config/schema.json; services/matching/core_v2.py» |
| ✅ | **23.2.1** #1 Не сводить всё к одному match_score | реализовано | tier/coverage/lcb/reciprocal/band хранятся и отдаются раздельно в карточке core_v2.search; единого match_score нет. — «services/matching/core_v2.py:search» |
| 🔒 | **23.2.2** #2 Не выводить semantic tier из score | в движке (sha-pinned) | assign_tier в закреплённом core_v2 строит тир из provenance (own-intent reciprocal / topical overlap), а не из score; покрыт тестом C23. — «services/matching/core_v2.py:assign_tier» |
| 🔒 | **23.2.3** #3 Не считать unknown совпадением | в движке (sha-pinned) | В directional_score unknown добавляет доменный prior в accumulator, но не увеличивает keyword-вес → снижает coverage, а не создаёт value совпадения (тест C1/C2). — «services/matching/core_v2.py:directional_score» |
| ✅ | **23.2.4** #4 Не смешивать safety/trust/fairness/payment с relevance | реализовано | Hard-gates отделены от скоринга; в конфиге ranking_boost_allowed:false; платёж/подписка в relevance не входят. — «config/Kleal_Matching_Core_Config_v2.yaml; services/matching/app.py:_hard_gates» |
| ✅ | **23.2.5** #5 Не использовать embeddings как финальную истину | реализовано | Эмбеддингов в скоринге нет вовсе — истина детерминированная таксономия/фичи; ANN/vector-пул отмечен blocked_infra и не используется. — «services/matching/core_v2.py:build_features» |
| ✅ | **23.2.6** #6 Не позволять LLM обходить hard gates | реализовано | Hard-gates выполняются до скоринга и повторно в _negotiate_precheck перед каждой отправкой; LLM только парсит запрос и ведёт переговоры, не влияя на гейты (тесты NEG1-4). — «services/matching/app.py:_negotiate_precheck» |
| 🟡 | **23.2.7** #7 Не вести свободные agent-to-agent диалоги вместо protocol events | частично | kleal_protocol.py вводит типизированные protocol-события и precheck-гейты, но negotiate_one всё ещё ведёт свободный LLM-диалог агента B, который и выносит accept/reject — переговоры не сведены к чистым событиям. — «services/matching/app.py:negotiate_one; shared/kleal_protocol.py» |
| ⛔ | **23.2.8** #8 Не ранжировать группы средним pair score | pilot-выкл (scaffolding) | kleal_groups скорит группу как SET: least_misery (min directed) + role_coverage + time_overlap + diversity, а не как среднее pair-матчей; мodule реализован но групповой поток pilot-disabled. — «shared/kleal_groups.py:least_misery/mean_pair_fit» |
| ✅ | **23.2.9** #9 Не переносить dating data в другие режимы | реализовано | infer_domain изолирует dating; hard-gate datingOk; dating-конфиг под release_gate=separate track — данные dating не попадают в prof/social ранжирование. — «services/matching/core_v2.py:infer_domain» |
| 🟡 | **23.2.10** #10 Не показывать проценты без калибровки | частично | Наружу (profile/карточка) отдаётся качественный band, процент не показывается; но в card-контракте search остаётся некалиброванное поле score=round(lcb*100,1) как внутренняя величина. — «services/matching/core_v2.py:search/assign_band» |
| 🟡 | **23.2.11** #11 Не хранить веса в нескольких местах | частично | v2-веса/пороги живут только в sha-pinned YAML (single source для core_v2), но legacy-скорер в app.py держит отдельный изменяемый словарь WEIGHTS + endpoint /api/agent/weights — второй источник весов сохраняется. — «services/matching/app.py:WEIGHTS; config/Kleal_Matching_Core_Config_v2.yaml» |
| ✅ | **23.2.12** #12 Не отправлять proposal без receiving policy и revalidation | реализовано | _negotiate_precheck обязательно ре-резолвит кандидата из live-стора, повторно прогоняет hard-gates и проверяет receiving-policy/readiness перед каждой отправкой (тесты RCV1-9). — «services/matching/app.py:_negotiate_precheck» |
| 🟡 | **23.2.13** #13 Не выполнять write без idempotency/version | частично | kleal_states даёт compare_and_swap (CAS по monotone version) и dedup (idempotency-ключ), покрытые тестами C14-CAS/C14-DEDUP на dict-объектах; но живые HTTP write-endpoints (negotiate/feedback) эти примитивы не применяют — реального version/idempotency на записи нет. — «shared/kleal_states.py:compare_and_swap/dedup; services/matching/app.py» |
| ✅ | **23.2.14** #14 Не повышать ranking за подписку | реализовано | ranking_boost_allowed:false в конфиге; логики платежей/подписки в скоринге нет. — «config/Kleal_Matching_Core_Config_v2.yaml» |
| 🔒 | **23.2.15** #15 Не использовать точную live location в discovery | в движке (sha-pinned) | core_v2._latlon первым предпочитает coarseLat/coarseLon, расстояние по haversine; explore_plans оффсетит координаты реальных юзеров — точная live-локация в discovery не используется. — «services/matching/core_v2.py:_latlon; services/matching/app.py:explore_plans» |
| 🟡 | **23.3** Definition of Done для любой фичи (12 пунктов) | частично | Выполнены: unknown/na handled, unit+property тесты (C1-C3), privacy/safety тесты (C5/C15/NEG/RCV), доменные фикстуры (R1-7/C4/C23), degradation (LLM+config fallback), UX-copy отделён от решения, config_version в trace. Частично/нет: typed I/O только на config (нет доменных TS-контрактов), reason keys — ru/en-фразы а не канонические keys, purpose/policy/model версии не логируются, race/idempotency тест лишь на state-примитивах а не на write-flow, dashboard-полей и observable events нет. — «services/matching/test_core_v2.py; services/matching/core_v2.py:search» |
| 🟡 | **23.4.1** #1 Сначала contracts и config validator | частично | config + load_config-валидатор (sha-pin + кросс-проверки evidence_id/version) реализованы; kleal_contracts.py даёт data-контракты в Python, но не .ts как в §23.1. — «services/matching/core_v2.py:load_config; shared/kleal_contracts.py» |
| 🟡 | **23.4.2** #2 Затем policy engine и state machine | частично | Policy реализован как _hard_gates (бинарный ALLOW/BLOCK); state machines реализованы в kleal_states.py (4 автомата, CAS, dedup); единого policy-engine с REVIEW/disclosure-слоем нет. — «services/matching/app.py:_hard_gates; shared/kleal_states.py» |
| ✅ | **23.4.3** #3 Затем compiler/retrieval/feature builder | реализовано | parse_intent (+fallback), load_candidates и build_features присутствуют и работают в связке. — «services/matching/core_v2.py:build_features; services/matching/app.py:parse_intent» |
| ✅ | **23.4.4** #4 Затем deterministic relevance и transparent results | реализовано | directional_score полностью детерминирован (без randomness/LLM), результаты прозрачны через slate/bands/reasons. — «services/matching/core_v2.py:directional_score/_slate» |
| 🟡 | **23.4.5** #5 После этого orchestration/proposals | частично | negotiate_candidates + _negotiate_precheck реализуют оркестрацию отправки с волнами и гейтами; reservation-объектов и idempotency на orchestration-слое нет. — «services/matching/app.py:negotiate_candidates» |
| ⛔ | **23.4.6** #6 Group formation — отдельный модуль | pilot-выкл (scaffolding) | Group formation вынесена в отдельный модуль shared/kleal_groups.py (set-constraints/GroupUtility/form_group), но групповой поток pilot-disabled. — «shared/kleal_groups.py» |
| 🟡 | **23.4.7** #7 ML-интерфейсы заранее, без подмены rule-based случайными вероятностями | частично | kleal_ml_boundary даёт детерминированный ML-интерфейс-заглушку без случайных вероятностей; всё ранжирование остаётся rule-based (конфиг помечает acceptance_probability absent в MVP), но полноценных заранее созданных ML-интерфейсов немного. — «shared/kleal_ml_boundary.py» |
| 🟡 | **23.4.8** #8 На каждом этапе запускать fixture suite и сохранять decision traces | частично | Fixture suite (test_core_v2.py, 264 проверки) запускается; decision trace формируется в карточке/explain_match, но отдельно по каждому прогону traces не персистятся. — «services/matching/test_core_v2.py; services/matching/core_v2.py:search» |
| ⬜ | **24.0** Самооценка спецификации (таблица 10 критериев, итог 98/100) | не реализовано | Это текстовая самооценка документа-спеки, а не поведение системы — в коде ничего не реализует; docs/SPEC_COMPLIANCE.md ведёт отдельный AS-IS учёт, но саму таблицу §24 не воспроизводит как артефакт. — «docs/SPEC_COMPLIANCE.md» |
| ⬜ | **24.1** Два незакрытых пункта (правовая оценка в Испании + эмпирическая калибровка порогов/моделей) | не реализовано | Оба пункта — обязательные pre-launch процессы вне кода: правовой оценки нет, эмпирической калибровки порогов/моделей нет (все пороги — статические значения sha-pinned конфига, модели отсутствуют). — «config/Kleal_Matching_Core_Config_v2.yaml» |

## Приложения A-E (config, псевдокод, acceptance tests, purpose-binding matrix, источники)

| | Подпункт | Статус | Что сделано (как есть) |
|---|---|---|---|
| 🔒 | **A.0** Canonical configuration YAML | в движке (sha-pinned) | Файл config/Kleal_Matching_Core_Config_v2.yaml существует, его sha256 = 21505ccb4add960291a742084b36d25289ffc93c9870a80b8cba3295010e9c5b (совпадает со спекой), содержит все ключи (score_semantics, feature_groups, 11 domains с весами, semantic_tiers, outreach, group_formation, user_facing_bands, currency_or_paid_priority) и грузится/валидируется через core_v2.load_config + config/schema.json (тест V1 sha-gate, V2 weight-sum). — «config/Kleal_Matching_Core_Config_v2.yaml; core_v2.load_config» |
| 🟡 | **B.1** Псевдокод search pipeline | частично | Реальный флоу в app.py воспроизводит шаги псевдокода (retrieval load_candidates → gates _hard_gates/_policy_decision с ALLOW/BLOCK/REVIEW → скоринг core_v2 → reciprocal combine → allocation rerank → transparent slate → _request_snapshot как audit), но это не буквальный per-tier цикл с config.budget[tier]/enough_strong/expansion.allowed-break: расширение по тирам вынесено в отдельную §12 expansion-ладдер, а не в единый цикл с ранним выходом. — «services/matching/app.py:_policy_decision,load_candidates,_request_snapshot» |
| 🟡 | **B.2** Псевдокод proposal acceptance | частично | Логика accept реализована детерминированно в kleal_states.py (assert_transition, compare_and_swap optimistic CAS с version-bump, idempotency dedup→DUPLICATE, capacity claim_slot, policy revalidate→POLICY_CHANGED, 8 race-кейсов) и покрыта тестами C14-*, но сама proposal-машина помечена как plan pilot-disabled, а outbox.publish('match_state_changed') реального event-outbox не имеет — это детерминированный in-memory scaffolding. — «shared/kleal_states.py:compare_and_swap,resolve_race; test_core_v2 C14-CAS/DEDUP/RACE» |
| ⛔ | **B.3** Псевдокод group formation | pilot-выкл (scaffolding) | shared/kleal_groups.py реализует все шаги (filter hard-set constraints, generate role-complete seeds, greedy_marginal_add, local_repair, set_feasible, group_utility, reserve_members) и покрыт тестами C15G-01..19, но модуль pilot-disabled: form_group/run_group_formation возвращают enabled:False и работают только под точным enabled_override; в person-to-person флоу не подключён. — «shared/kleal_groups.py:form_group,group_utility; test_core_v2 C15G-05/09/11» |
| ✅ | **C.1** Sparse не обгоняет full без low-coverage отметки | реализовано | Тесты C1a-C1e подтверждают: оба direct-кандидата всплывают, полный профиль выше по lcb, sparse имеет ниже coverage + список unknown, помечен (band != especially_close) и стоит в слейте после full. — «test_core_v2.py:71-77 (C1a-e)» |
| ✅ | **C.2** not_applicable не уменьшает coverage | реализовано | Тест C2: online-интент (location = not_applicable) даёт coverage выше, чем offline с unknown-geo — NA-фичи исключаются из знаменателя coverage в core_v2. — «test_core_v2.py:84 (C2)» |
| ✅ | **C.3** Один raw evidence не даёт двойной вес | реализовано | Тест C3: алиасы одного интереса дают идентичный lcb (разница <1e-9) — одна evidence не начисляется одновременно как exact/tag/category/embedding. — «test_core_v2.py:93 (C3)» |
| ✅ | **C.4** T2 не становится T1 из-за хорошего времени | реализовано | Тест C4: sibling-кандидат остаётся T2 несмотря на идеальные time/geo — тир категориальный (provenance), не выводится из score. — «test_core_v2.py:118 (C4)» |
| ✅ | **C.5** Safety block исключает до feature building | реализовано | Тесты C5a-C5e: paused/under-18/сам искатель никогда не скорятся, годный проходит гейты в core, wrapper возвращает v2-поля — гейты стоят перед скорингом. — «test_core_v2.py:134-138 (C5a-e)» |
| ✅ | **C.6** Смена privacy между SENT и ACCEPT -> POLICY_CHANGED | реализовано | Реализовано в _revalidate/kleal_states: тест C8-REVAL даёт POLICY_CHANGED когда live-стор строже базлайна, а C14-RACE маппит privacy_tightened/policy_revoked_before_accept на error_code POLICY_CHANGED. — «test_core_v2.py:877 (C8-REVAL), 1313 (C14-RACE)» |
| ✅ | **C.7** Повторный accept с тем же idem key -> тот же результат | реализовано | Тесты C14-DEDUP и C14-TRANSITION: idempotency-ключ композирует action, реплей возвращает сохранённый прежний результат как DUPLICATE (применено ровно один раз). — «test_core_v2.py:1274 (C14-DEDUP), 1381 (C14-TRANSITION)» |
| ⛔ | **C.8** Два concurrent accept последнего слота не превышают capacity | pilot-выкл (scaffolding) | Гонка за последнее место реализована в kg.claim_group_seat поверх ks.claim_slot и покрыта тестом C15G-08 (ровно один побеждает, второй SLOT_TAKEN, claim >= capacity -> CAPACITY_EXCEEDED), но живёт в pilot-disabled групповом модуле. — «test_core_v2.py:1467 (C15G-08); shared/kleal_groups.py:claim_group_seat» |
| ✅ | **C.9** Expired proposal нельзя принять | реализовано | EXPIRED — терминальное состояние proposal-машины (kleal_states MACHINES), выход из терминала запрещён (C14-EDGES deny-safe), а ks.reservation_expired детерминированно вычисляет истечение (C14-RESERVATION-TTL). — «shared/kleal_states.py:48; test_core_v2.py:1242,1285» |
| ✅ | **C.10** Counter после withdrawal не открывает match | реализовано | WITHDRAWN — терминальное состояние без исходящих рёбер, любой переход (в т.ч. counter) из него отклоняется deny-safe в C14-EDGES/apply без мутации объекта. — «shared/kleal_states.py:48; test_core_v2.py:1242,1260» |
| ✅ | **C.11** Subscription flag не меняет relevance/позицию | реализовано | app._payment_invariant читает currency_or_paid_priority и энфорсит ok=(no ranking boost)&&(no safety effect); тесты PAY1/PAY2 подтверждают, что включение boost дало бы ok=False; в скоринг платёжный сигнал не подаётся вовсе. — «services/matching/app.py:_payment_invariant; test_core_v2.py:382 (PAY1/2)» |
| ✅ | **C.12** Dating preferences недоступны friendship search | реализовано | build_profile_view purpose-bound через DOMAIN_TO_CONTEXT+PURPOSE_FIELDS: тесты C4-PV1/PV4/INT2 подтверждают, что friendship-view исключает datingOk, и ни один контекст его не раскрывает. — «shared/kleal_contracts.py:build_profile_view; test_core_v2.py:496,504,544» |
| 🟡 | **C.13** Friendship decline не обучает dating ranker | частично | Отдельного обучаемого dating-ранкера нет (движок 100% детерминированный), а feedback-loop record_feedback хранит accept/reject по имени в одном общем kleal_store без разбивки по purpose/домену — изоляция обучения по назначению как отдельная гарантия не реализована, хотя обучаемого ранкера, который мог бы загрязниться, не существует. — «services/matching/app.py:record_feedback:339» |
| ✅ | **C.14** Exact live location не входит в discovery payload | реализовано | _PRECISE_LOCATION_KEYS включает live_location/exact_lat/home/work/address и вычищается из каждой возвращаемой карточки (app.py:1297); тест C8-SCRUB подтверждает отсутствие точных координат в payload. — «services/matching/app.py:684,1297; test_core_v2.py:901 (C8-SCRUB)» |
| ✅ | **C.15** Dota intent не шлётся LoL player без broad consent | реализовано | Тесты C15a/C15b: parent/sibling-кандидат (T2) не получает personal outreach без broad-matching consent, а флаг consent не меняет тир; поддержано semantic_tiers в конфиге (T2 personal_outreach: requires_broad_matching_consent). — «test_core_v2.py:147-149 (C15a/b)» |
| 🟡 | **C.16** Spanish learner не получает learner как native при hard role | частично | Есть hard-гейт requiredLanguages (C5-GATE1 блокирует не-носителя) и directed_preferences по роли в core_v2, но выделенного теста именно на learner-vs-native комплементарность роли в language_exchange нет — сценарий покрыт косвенно через language-гейт и role-скоринг. — «test_core_v2.py:582 (C5-GATE1); core_v2.py:260 directed_preferences» |
| 🟡 | **C.17** Founder target не проверяется по self профессии инициатора | частично | directed_preferences в core_v2 (стр.260) сравнивает целевую роль ИНТЕНТА с ролью кандидата и включается только когда интент реально таргетит роль, то есть self-профессия не используется как implicit preference — соответствует требованию, но отдельного именованного acceptance-теста на этот кейс нет. — «services/matching/core_v2.py:260-272» |
| ⛔ | **C.18** Padel group с 4 одинаковыми role/side отклоняется | pilot-выкл (scaffolding) | Отклонение непокрытой mandatory-роли реализовано (form_group -> group None при незакрытой обязательной роли, C15G-07) и per-set hard-constraints (role packs, C15G-16/18), но всё в pilot-disabled модуле kleal_groups под override. — «test_core_v2.py:1454 (C15G-07),1526 (C15G-16)» |
| ⛔ | **C.19** Группа с pairwise block отклоняется | pilot-выкл (scaffolding) | Feasibility доминирует над скором: одна hard-violation даёт utility None + код нарушения (C15G-04), per-member/set hard-constraints фальсифицируют набор независимо (C15G-18) — реализовано, но в pilot-disabled kleal_groups. — «test_core_v2.py:1436 (C15G-04),1538 (C15G-18)» |
| ⛔ | **C.20** Event и user: разные transactions и explanation copy | pilot-выкл (scaffolding) | kleal_candidates моделирует Event/Room/Venue как отдельные типы (event != user-with-capacity, directed-only, C16-EVENT-NOT-USER), каждый объект несёт is_personal_match:False/is_alternative:True и свою event_transaction, но модуль pilot-disabled (enabled:False, работает только под override). — «shared/kleal_candidates.py:488; test_core_v2.py:1573,1586» |
| 🟡 | **C.21** LLM outage не отменяет policy gates | частично | Все гейты и скоринг keyless/LLM-free (core_v2, _hard_gates, kleal_states — C14-STATES-KEYLESS), поэтому политика работает без LLM по построению, но отдельного теста-симуляции именно LLM-outage для гейтов нет (LLM применяется только к парсингу free-text запроса вне гейтов). — «services/matching/core_v2.py; test_core_v2.py:1329 (C14-STATES-KEYLESS)» |
| ✅ | **C.22** Vector outage сохраняет structured retrieval | реализовано | Тест C7-STAGES: все 7 стадий §7.2 присутствуют, ANN-recall честно помечен blocked_infra, а структурный retrieval продолжает выдавать кандидатов. — «test_core_v2.py:798 (C7-STAGES)» |
| ✅ | **C.23** Reason keys не содержат несуществующих фактов | реализовано | Тест C23 подтверждает отсутствие выдуманных reason-ключей для unknown-групп (объяснения строятся только из известной evidence). — «test_core_v2.py:156 (C23)» |
| ✅ | **C.24** Replay воспроизводит результат той же версии конфига | реализовано | Тест C24: идентичные входы реплеятся в байт-идентичный слейт; версии зафиксированы в _request_snapshot (intent/config/policy/data). — «test_core_v2.py:161 (C24); app.py:_request_snapshot» |
| 🟡 | **D.0** Матрица purpose binding | частично | Field-level purpose-binding закодирован как DOMAIN_TO_CONTEXT + PURPOSE_FIELDS allow-lists и build_profile_view (deny-safe для немапнутых доменов, C4-PV1..4, C4-ENUMS), но это не полный перенос 10-строчной матрицы: тонкие режимы вроде staged disclosure, eligibility-band vs точный возраст по consent, coarse/venue реализованы лишь частично (coarse-локация, disclosure-ладдер §8.4), без покрытия каждой ячейки таблицы. — «shared/kleal_contracts.py:71-176 (DOMAIN_TO_CONTEXT/PURPOSE_FIELDS/build_profile_view)» |
| ⬜ | **E.0** Источники и нормативные ориентиры | не реализовано | Это библиография (GDPR, EU AI Act, EDPB, reciprocal-recommendation papers, PostGIS/H3/pgvector) — не исполняемое требование; отдельного кода/артефакта нет, хотя принципы purpose-limitation/минимизации отражены в §4/§8 purpose-binding и location-scrub коде. — «n/a (bibliography)» |
